# R20–R23 validation notes

2026-09-18 UTC. Shipped campaign/protected provenance checks pass:51 protected bytes unchanged;23 documented mutable paths refreshed. The supplemental outside-repository manifest scanner initially reported29 missing/hash entries for AUD060 FINAL_SHA256SUMS.txt. Its actual manifest is relative to the sibling report directory, exactly as its full issued verifier and README prescribe; the generic scanner incorrectly tried only repository and manifest-directory bases. The issued portable AUD060 verifier had already passed every exact byte and17 detecting integrity controls. Root changed only the supplemental scanner's explicit resolver for this one manifest to parent.parent, matching the same documented rule already used for AUD055. No source, checksum, sealed packet, shipped verifier or protected byte was changed to clear the failure. Rerun outcome is recorded in the current pre-stage validation JSON.

AUD060 report's generic ZIP wording is separately clarified: actual archive is tar.gz and is verified by streaming tarfile without extraction. Original evidence bytes remain unchanged. Root's unsealed comparison draft briefly contained an erroneous hostile line count261; direct wc showed234 and the draft was corrected before issuance/publication. This was a record-count correction, not a mathematical change.

AUD062's archive manifest uses inputs/ and outputs/ archive namespaces, as the issued verifier explicitly constructs them. The supplemental scanner maps outputs/ back to the canonical report/payload paths for this exact manifest; inputs/ remain packet-relative. Every referenced digest is checked. No generic bypass or checksum weakening.

The first fresh AUD062 code-only reproduction compared stdout bytes with its saved JSON and failed: the program writes a newline-terminated JSON string to RESULTS.json but print(rendered) appends one further newline to stdout. Root repeated the documented --write only inside a new temporary code-only directory and compared the generated RESULTS.json against the sealed saved bytes. No mathematical assertion failed and no issued output was normalized or changed. The final rerun record states the exact byte comparison and print-newline distinction.

Final R20/R21 staging: the diff was whitespace-clean before adding the verbatim TeX build log. That log alone introduces five warnings at lines292,345,348,371 (trailing whitespace) and378 (blank EOF); these are exact generated evidence bytes and are deliberately preserved. All selected staged blobs are checked byte-for-byte against the reviewed working files. Future R22/R23 proof/task/packet payloads are held outside this accepted-gate commit; chronological canonical state mentions remain.
