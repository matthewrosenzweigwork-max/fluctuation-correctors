# R18/R19 validation notes

2026-09-18 UTC. The first supplemental aggregate scan found20 apparent missing/hash failures in the AUD055 output manifest. Its declared names are relative to the report directory, as specified in its README and independently passing original verifier. The supplemental scanner had supported only repository-root and manifest-directory paths. Root added an exact-manifest-specific report-directory resolver to the temporary scanner, without modifying any issued source, manifest, original verifier or required campaign check, then reran the full aggregate scan. This is a supplemental scanner path-base correction, not a waived byte mismatch.

Post-publication review of e75f8b7 whitespace finds the six evidence/build warnings recorded before the final preflight was added, plus five exact duplicate trailing spaces inside that preflight's literal quoted output, at its lines15,17,19,21,24. The preflight's six-count refers to its recorded pre-addition check. All11 current commit warnings are understood and preserved; no mathematics or issued packet bytes changed. Future preflight records will describe locations without copying trailing-space lines and the final full staged tree will be checked again after adding the record.

After the explicit AUD055 path-base correction, aggregate scan passes223 manifests/seals and2456 digest entries across153 then-pending paths, zero errors. Protected51 immutable/23 documented mutable and shipped campaign verification also pass. R18/R19 draft synthesis initially built3pages205954bytes with full log read and all3pages visually checked; the short trailing page led to a compact title/margin adjustment. Revised build is currently2pages (see current log); final accepted-status rebuild/render remains required after hostile gates. No pending command process.

The compact draft build exposed one19.12212pt overfull inline fluctuation definition; moving that exact definition to a display resolved it. Current pending-gate draft is2pages189143bytes; all diagnostic log lines are clean and both rendered pages have been visually inspected. PDF remains under BUILD until final accepted-status build. Old3-page render page-3.png is a stale draft artifact in ignored BUILD and must not be included in the final two-page review.
