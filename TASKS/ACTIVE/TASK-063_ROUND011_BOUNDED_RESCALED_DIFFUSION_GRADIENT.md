# TASK-063 — uniform weighted gradients at bounded rescaled diffusivity

Astra Max proof construction, new isolated codex/hocf-r011-rescaled-gradient worktree from published R8 b2510ed08ecc26387d9fc14daaf174d3c35f5638. Copy/hash-check only AUDITS/ROUND_011_RESCALED_GRADIENT_INPUT_SHA256SUMS.txt. No ongoing R9/R10 audit or proof, canonical state/history, memory, prior checkers, root edits, dependencies, commits, pushes or children. The following is an explicitly unproved root seed, not independent certification.

Fix the actual homogeneous unit-Haar gradient Riesz model,d>=3,0<s<=d-2,T finite,smooth real h and full symmetric terminal-zero pair inverse with both responses. Put p=s+2 and chi_N=nu N^(2/p). For a fixed finite L>=0, primary assertion: uniformly over every N>=2 and0<=chi_N<=L, sup_t[ sup_(x!=y)|Phi_t^N|/w_s(x-y)+sup_(x!=y)|grad_pair Phi_t^N|/w_(s+1)(x-y)]<=C(d,s,T,h,L,kernel,weights), independent of N and nu. The weights are fixed smooth positive periodic weights agreeing with r^-q near zero, bounded elsewhere. All gradients are actual continuous off-diagonal derivatives of the same full inverse, with the global weak W1,1 representative. A bound for the contracted gradient A_t(x)=integral grad_x Phi_t(x,y)dy must follow with an N-independent constant. Zero noise is included. Negation: fixed admitted data and a bounded-chi sequence with unbounded normalized weighted norm or failure of the stated derivative/representative assertion.

Root seed to check: the weighted first-flow-moment inequality contains positive2nu alpha(alpha+2-d)_+ r^-2 minus a positive multiple of N^-1 r^(-s-2), with alpha>moment order. Its exact supremum is at most a constant times N^(2/s)nu^((s+2)/s)=chi_N^((s+2)/s). Thus the previously fixed-N exponential cost may become uniform when chi is bounded. Rebuild every needed high-moment/UI/common-start step with this uniformity; do not simply relabel an unspecified C_N as uniform. For source J, weighted value/gradient exponents s and s+1 are natural; s+1<d permits global L1. Check both finite-measure responses and their Coulomb compensation, weak derivative transfer, source expectation derivatives and factorial Volterra identification. No diagonal trace or H1 bound follows from this particular singular weight by itself.

This is an explicit subrange module, not a replacement for PO024's full bounded-noise target. Under microscopic criticality lambda_N=beta_N N^(s/d-1) bounded below, chi_N=lambda_N^-1 N^[s(s+2-d)/(d(s+2))] is bounded in this d>=s+2 range. Verify that exponent exactly. Do not conflate the old energy-floor condition with full subcritical or critical coupling. General fixed-positive nu has unbounded chi and is excluded from this new uniform claim, though prior fixed-N domain results still apply.

Attempt a complete proof or exact failure, with uniform constants and independent exact radial/exponent diagnostics. If the primary bound passes, investigate and prove only if justified the natural local C1 and global W1,1 convergence as N tends to infinity in this bounded-chi class to the terminal-zero response-only kernel integral_0^(T-t) exp(rR)J^0_(t+r)dr, where f^0 is the actual zero-noise Fourier test. Do not assert weighted-sup convergence near the shrinking singular region or any actual-law singular tail. The convergence extension requires its own precise quantifiers and proof, not a formal dropping of nu Delta+B/N.

Write MEMORANDA/ROUND_011_BOUNDED_RESCALED_DIFFUSION_GRADIENT.md with complete assertion/negation, source preflight, proof attempt, exact surviving/failing line and limitations; independent checker/results, README and immutable input/output/archive seals. No canonical theorem ID until root integrates. Work independently of the actual-law tail attempts; this module supplies spatial control and cannot itself transfer an iid reference estimate to an evolved law.
