# TASK-045 — finite-N singular particle realization and heat passage

2026-09-17. Astra Max constructor in a new isolated codex/hocf-r006-particle-domain worktree from published R3 52bda5d0d24067b051c6fe9763f2a78e7599e593 plus explicitly supplied sealed prerequisites. Root alone integrates; no child agents, commits, pushes, dependencies or canonical edits. This bounded next task addresses an actual N-particle prerequisite to the corrector domain, not another broad plan.

Exact assertion to construct or falsify: on the unit Haar torus, d>=3,0<s<=d-2, frozen Fourier Riesz g,K=-gradg, b=-gradV with fixed smooth periodic real V, N>=2 and every finite nu>=0,T, the SDE

    dX_i=[b(X_i)+(1/N)sum_(j!=i)K(X_i-X_j)]dt+sqrt(2nu)dW_i

has a unique global measurable pathwise realization from each configuration with all coordinates distinct, with no pair collisions. Heat-regularized paths driven by the same Brownian motions converge uniformly on each finite horizon almost surely for each fixed starting configuration/tuple. Joint measurable Markov laws are constructed, not inferred from a formal generator. For initial bounded N-body density F0, prove the explicit Lebesgue density domination

    F_t <= ||F0||infty exp{[N a+(N-1)kappa]t},

where divb>=-a and divK>=-kappa in the finite-measure sense (a,kappa>=0). For iid mu0<=M the bound is M^N times this exponential. It is a fixed-N domination, not an N-uniform iid-moment or fluctuation estimate. Exact negation: an admissible tuple/start/density violates an included assertion. If true, also derive exact finite-N stopped energy/exit bounds with constants and integrability for bounded-density initial data. Show explicitly whether a pathwise uniform lower pair distance on a finite interval is available for each realization.

Root proposed route, unproved seed: H_N=sum_i V(x_i)+(1/N)sum_(i<j)g(x_i-x_j) generates the exact drift -grad H_N. Away from collisions, Delta_(Nd) H_N=sum_i DeltaV+(2/N)sum_(i<j)Delta g <=C N. Thus the stopped generator of H_N is -|gradH_N|^2+nu DeltaH_N. Shift by an explicit O(N) constant to obtain a nonnegative Lyapunov function that diverges at every partial collision. Establish stopped expectation/exit estimates without assuming uniform integrability or global Ito on a singular function. Compactness excludes other explosion. Check that simultaneous collisions and smooth periodic remainders cannot defeat the lower bound. For heat passage use local convergence of K_epsilon in C1 away from collisions, pathwise cutoff/Gronwall and a noncollision bootstrap. For density domination, compute the Nd divergence before estimating, obtain a smooth random-flow Jacobian bound, and pass measure inequalities to the singular limit using bounded continuous tests and an explicit measure extension.

Permitted inputs: task, AGENTS, frozen R1 model and THM021's sealed proof/card for local Fourier normalization, finite D and lower bound. The supplied R5 periodic base proof may be used only as an explicitly declared analogous method if listed in the input manifest; it is not an N-particle theorem. No unsealed worker outputs, root later notes or unrelated state/audits/memory. Freeze the actual read dossier. Write complete MEMORANDA/ROUND_006_SINGULAR_PARTICLE_REALIZATION.md, a reproducible exact independent coefficient/solvable-model checker if useful, and input/output SHA256 manifests. Test N=2,3,nu=0,constant V,Coulomb threshold and multiple-close-pair configurations. Do not import a singular SDE theorem without a verified primary statement; a direct construction is preferred.

Acceptance is only this finite-N realization/passage/density result with exact hypotheses and constants. It does not show that the bounded Borel pair inverse has gradients, belongs to the interacting N-particle generator domain, has a valid singular Ito identity, or satisfies N-uniform evolved residual/bracket bounds. Identify the exact next unsupported domain line after this task. A fresh reconstruction and hostile review are required after seal. On failure return the precise first unsupported line and strongest surviving statement.
