# R19 root uniform-diffusivity construction packet

Status: SELF_CHECKED root construction; whole independent gates pending. Exact proof is ROUND_019_UNIFORM_DIFFUSIVITY_GAUSSIAN.md, also adjacent to this directory. Full statement and12 source inputs are frozen under INPUTS. EXPOSURE.md discloses all root/progress contamination; this is never a blind context.

From repository root run:

    python3 MEMORANDA/ROUND_019_UNIFORM_GAUSSIAN_ARTIFACTS/round019_exact_checks.py --output /absolute/new/output.json

Use a new output outside this immutable packet. PASS:22623 assertions,27 categories,9 mutation families. No random seed/tolerance or external dependencies. The analytic proof establishes the singular/uniform limit; rational parameter-dependent modal times only test algebra.

    python3 MEMORANDA/ROUND_019_UNIFORM_GAUSSIAN_ARTIFACTS/verify_packet.py

The verifier checks original input copies against declared digests, exact payload membership, all output bytes, adjacent proof identity, diagnostic/code correspondence, ZIP hash/membership/bytes and safe regular member names without extraction. OUTER_SEAL.json hashes the output manifest and archive; these external files are excluded from the archive to avoid self-reference. Read-only permissions supplement hashes; Git does not preserve arbitrary permission bits. Corrections require separate artifacts. No TeX file is claimed in this packet.
