# Round012 root actual-noise candidate

ROOT SELF_CHECKED, not independently certified. The full proof is ../ROUND_012_SUBCOULOMB_ACTUAL_NOISE.md. It proves a candidate quantitative gradient power and, conditional on the newly constructed R10 sharp energy floor and the supplied earlier modules, an actual full martingale bound in a strict sub-Coulomb range. No full fluctuation theorem or cubic-residual closure. Fresh Ultra TASK066 receives the frozen statement and prior sources but not this proof; a separate hostile review remains required.

The root coordinator has the full campaign context, including prior source, construction and review histories. The mathematical-source manifest is not a claim of isolated exposure. The current R11 worker proof was not read or used; its theorem is not a prerequisite. Both R10 reports were read after their independent seals, and their fresh hostile reviews were still unissued when this root construction was written. All root seeds are exposed in the complete proof rather than labeled as independent evidence.

The newly written round012_noise_bound_exact.py uses only standard-library exact fractions. It checks351 mixed-sign configuration bounds,1053 physical prefactor cases,210 strict exponent tuples,10 Coulomb exclusions and3 explicit boundary controls:1627 exact cases, each potentially containing multiple assertions. These arrays are algebraic diagnostics, not actual particle laws. No random seed, tolerance or prior-checker import. The first run passed; the metadata label was clarified from assertions to exact_cases before sealing and the second run passed. This finite self-check does not prove the analytic theorem.

Verify source/output SHA256 manifests from root and the seal from this artifact directory. Extract a separate packet copy before rerunning python3 MEMORANDA/ROUND_012_NOISE_ARTIFACTS/round012_noise_bound_exact.py, which writes its result beside itself. Preserve issued bytes. The sealed archive includes exactly the named mathematical inputs, named outputs and output manifest; it excludes itself and the outer seal. Later repairs must be new artifacts.
