# TASK-084 — sealed bounded-noise Gaussian construction

This packet supplies the complete conditional construction of the entire unchanged THM040 and its exact negation. It includes the genuine singular first-order passage, actual R10-based bracket concentration, triangular initial tests, initial/thermal dependence, full joint weak limit, exact covariance and degeneracies.

Status: CONDITIONAL PROVED CANDIDATE / CONSTRUCTOR SELF-CHECK. Complete R16/R17 source statuses remain conditional as issued. No independent reconstruction or hostile gate is presumed or assigned.

Authoritative worktree memorandum: ../ROUND_018_BOUNDED_NOISE_GAUSSIAN.md. Its identical packet copy is ROUND_018_BOUNDED_NOISE_GAUSSIAN.md. SOURCE_PREFLIGHT.md identifies all imports and limitations; EXPOSURE.md records every input class and incidental metadata exposure; ASSERTION_DISPOSITIONS.md records the entire target/negation disposition; PROVENANCE.json and inputs/ preserve all fourteen frozen inputs.

The fresh exact diagnostic is round018_exact_diagnostic.py. It uses rational/Gaussian-rational sparse Fourier arithmetic, exact formal exponential polynomials and conditional Gaussian series. It reads no prior checker. The recorded result is PASS: 3150 exact assertions in 26 categories, with all 9 mutation families rejected. This supports coefficient/probability self-checks; the analytic memorandum supplies the conditional proof.

Run from the prescribed isolated worktree:

    python3 MEMORANDA/ROUND_018_BOUNDED_NOISE_ARTIFACTS/round018_exact_diagnostic.py --output /absolute/path/to/a/new/results.json

Use an output path outside this read-only packet for any rerun. The mathematical program has no external dependencies, randomness or floating-point tolerance. It covers N=2 through 5, zero and positive noise on both sides of one, constant and mixed Fourier tests, literal first-order/cross-bracket generators, initial variance and initial/time covariance derivatives, exact covariance identities, zero/repeated times, linear dependence, dependent conditional Gaussian moments, compensator sign and random-bracket replacement controls. The normalizations and limitations are in the JSON and memorandum.

The packet verifier is round018_verify_packet.py. With the sibling timestamped seal file:

    python3 MEMORANDA/ROUND_018_BOUNDED_NOISE_ARTIFACTS/round018_verify_packet.py --seal MEMORANDA/ROUND_018_BOUNDED_NOISE_SEAL_TIMESTAMP.json --source-root . --memorandum MEMORANDA/ROUND_018_BOUNDED_NOISE_GAUSSIAN.md

Replace TIMESTAMP with the issued stamp in the accompanying archive/seal names. The verifier checks all fourteen inputs, exact output membership and hashes, memorandum identity, program/result correspondence, archive digest and exact bytes, absence of duplicate/unsafe/nonregular/encrypted entries, and declared membership. It never extracts the archive. Ten unsafe-name negative controls are included. Optional --rerun-output /absolute/path/to/new/results.json executes the already-verified fresh diagnostic and requires the new record to match, without overwriting any existing output or sealed packet file.

INPUT_SHA256SUMS.txt is the exact supplied input manifest. OUTPUT_SHA256SUMS.txt covers every packet file except itself; its digest is in the external seal. ARCHIVE_CONTENTS.txt lists the exact prefixed archive members, including both manifests and itself. The external seal records the archive digest/size/member count, input/output manifest digests, proof digest and diagnostic digests. Sibling verification JSON records the executed safe exact-byte check. Read-only permissions are applied to final output, input-copy, archive and seal files. Any correction must be separately issued, not edited into this packet.

No TeX artifact was created or modified, so no LaTeX build is claimed. No external reference, unverified literature attribution, path-space theorem, broader campaign closure, canonical edit, commit, push, installation or child agent is part of this handoff.

Root alone integrates after fresh whole-claim reconstruction, separate hostile review and source-hypothesis comparison.
