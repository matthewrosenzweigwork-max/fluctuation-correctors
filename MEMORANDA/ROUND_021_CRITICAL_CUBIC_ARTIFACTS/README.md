# R21 root critical-cubic construction packet

SELF_CHECKED / independent whole gates pending. The adjacent memorandum is reproduced exactly here. Twenty-nine complete permitted inputs and their original histories are frozen under INPUTS. EXPOSURE.md discloses the root's extensive prior exposure; SOURCE_PREFLIGHT.md maps precise uses and limits.

From repository root:

    python3 MEMORANDA/ROUND_021_CRITICAL_CUBIC_ARTIFACTS/round021_exact_checks.py --output /absolute/new/output.json
    python3 MEMORANDA/ROUND_021_CRITICAL_CUBIC_ARTIFACTS/verify_packet.py

Use a new output outside this immutable packet. Expected PASS: 12531 assertions, 18 categories, four rejected-mutation families. Exact rational arithmetic; no seed/tolerance/dependencies. Analytic proof is in the memorandum and full sources, not the finite diagnostic.

The adapted byte verifier checks all input/output bytes, exact payload membership, adjacent proof identity, program/result correspondence, safe regular ZIP members and complete archive bytes without extraction. OUTER_SEAL.json hashes the output manifest and archive outside the archive to avoid self-reference. Read-only modes supplement hashes. No TeX artifact is claimed. Corrections require separately issued records.
