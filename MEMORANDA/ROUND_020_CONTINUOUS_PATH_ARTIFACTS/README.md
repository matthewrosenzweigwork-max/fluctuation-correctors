# TASK090 — complete continuous-path construction packet

Issued 2026-09-18 UTC. Entire unchanged THM042: **conditional proved candidate; constructor self-check only**. The independent whole-claim reconstruction and hostile gates remain for the root coordinator. Earlier source statuses are preserved as issued, with no inference from theorem cards.

The proof establishes the singular actual-law decomposition and exact Haar centering, a simultaneous-family source bound and expected path-supremum error tending to zero, uniform fourth moments for the continuous linear part, tightness of the actual laws in the uniform topology, a continuous real Fourier Gaussian series with the frozen covariance, and uniform-topology weak convergence. It includes zero noise and horizon, constants, dependent tests and all admissible endpoints. The complete mathematical argument and its independent falsification route are in `PROOF/ROUND_020_CONTINUOUS_PATH_GAUSSIAN.md`.

## Contents

- `INPUTS/`: exact fourteen frozen input files, retaining their original relative paths.
- `INPUT_SHA256SUMS.txt`: the exact construction input manifest.
- `PROOF/ROUND_020_CONTINUOUS_PATH_GAUSSIAN.md`: byte copy of the sibling authoritative worker memorandum.
- `SOURCE_EXPOSURE.md`: source uses, nondependencies, isolation and exposure record.
- `round020_path_diagnostic.py`: fresh exact standard-library diagnostic, reading no prior checker.
- `round020_path_diagnostic_results.json`: its deterministic actual output, including the program digest and every mutation witness.
- `verify_packet.py`: read-only exact-member, byte and hash verifier; it never extracts an archive.
- `OUTPUT_SHA256SUMS.txt`: hashes for all non-input packet files except itself. The outer archive digest and sibling seal cover this manifest's exact bytes.

The input and output manifests together account for every regular file; the output manifest itself is the sole explicit self-hash exclusion. The archive contains the entire packet at its named top-level directory and no other member. The outer sibling files are `ROUND_020_CONTINUOUS_PATH_ARTIFACTS.zip`, its `.sha256`, and `ROUND_020_CONTINUOUS_PATH_ARTIFACTS_SEAL.md`. The seal records the actual verification output and hashes, including the sibling memorandum and both manifests.

## Reproduce and verify

From the isolated worktree root:

```text
python3 MEMORANDA/ROUND_020_CONTINUOUS_PATH_ARTIFACTS/round020_path_diagnostic.py

python3 MEMORANDA/ROUND_020_CONTINUOUS_PATH_ARTIFACTS/verify_packet.py \
  --archive MEMORANDA/ROUND_020_CONTINUOUS_PATH_ARTIFACTS.zip \
  --memo MEMORANDA/ROUND_020_CONTINUOUS_PATH_GAUSSIAN.md \
  --rerun-diagnostic
```

The diagnostic result is **PASS: 4,255 exact assertions, 26 categories, 13 nonvacuous mutation controls rejected as intended**. It uses exact fractions and formal finite exponential polynomials. There is no randomness, floating-point tolerance, new dependency or trajectory simulation. The verifier reruns the included diagnostic and compares its entire output byte-for-byte with the saved result. It verifies every frozen input and output hash, exact packet and archive membership, safe relative paths, no links or special files, declared lengths and full member bytes, archive digest, and identity of the sibling proof. No extraction or outside reference is required. `--archive` and `--memo` may be omitted for a standalone packet-only verification; their corresponding checks are then explicitly absent from its output.

After successful verification the packet files, its archive, the sibling proof and seal are read-only. A correction must be issued as a new superseding artifact. Read-only permissions are a review convention, not a cryptographic signature; the exact byte digests identify this issue.

## Evidence limits and handoff

The computation checks modal covariance and path-increment algebra, old-noise convolution contributions, iid fourth moments, dyadic counts and source exponents. Exact tent paths disprove the each-time-to-supremum shortcut; an adapted Brownian example disproves replacing a random bracket by its mean in a fourth-moment identity. Those examples are tests of proof inferences, not admitted particle-law counterexamples. Rational modal rates are diagnostic parameters; the Riesz normalization is checked in the analytic proof. Computation does not certify singular passages or tightness.

The full supplied sources are conditional as issued. The packet includes no unseen R17 premise, no R10 pair-corrector domain or singular-gradient-tail premise, and no independent gate claim. There is no remaining local path-estimate gap in the conditional construction. Source validation and the two fresh independent whole gates remain the exact prerequisites to promotion.

Only the assigned worker memorandum, this directory and its sibling archive/seals were written beyond the permitted exact input overlay. No canonical edit, state/history or memory lookup, external search, prior checker read, child agent, dependency installation, commit or push occurred. Root alone may integrate and assign gate status. The requested mathematical deliverable is Markdown; no TeX source was created or edited, and no PDF or visual-rendering verification is claimed.
