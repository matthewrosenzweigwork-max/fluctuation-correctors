# R27 killed attractive duality — self-checked only

Read REPORT.md and CLAIM_CARD.md. THM052 is an unaccepted complete candidate; the original critical source is still open. The auxiliary attractive diffusion is killed at collision/explosion and conditioned on its rare survival event; it is never substituted as an unqualified original particle law. All ten exact source/task/claim copies, root exposure, run history and fresh diagnostic are included.

Run python3 /path/to/this/verify_packet.py with no arguments for read-only source/payload/archive/seal/report/mode checks. It never extracts or executes another file. Run diagnostic.py without arguments to print fresh deterministic JSON; do not redirect it over the issued result. For independent reproduction copy the program alone to a new temporary directory before executing. No dependencies beyond Python standard library.

PAYLOAD_MANIFEST.json hashes every packet file except itself. The sibling SEAL.json binds that manifest, exact external report and tar.gz. The archive contains every packet file, including the manifest, under one safe prefix. Thus there is no recursive self-hash. The seal is an integrity record, not a signature or mathematical audit. All issued files/directories are read-only; any correction requires a separately named artifact.
