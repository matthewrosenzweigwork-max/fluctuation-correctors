# Round021 — integrated critical cubic in the full strict R12 range

2026-09-18 UTC. ROOT CONSTRUCTION / SELF_CHECKED / VERSION_LOCKED. Exact THM043 conjunction and negation are frozen separately. This is exposed synthesis of already accepted complete modules, not independent reconstruction. The accompanying sealed packet records the source/exposure map and exact self-check. Both independent whole-claim axes are outstanding. No theorem promotion.

## 1. Scope and source map

Take exactly THM043. All conventions and actual singular objects are fixed there: homogeneous unit-Haar torus, coefficient-one Riesz, actual iid-Haar-prepared singular gradient law, ordered distinct labels/N^k, P=U2/2, genuine terminal-zero full pair inverse Phi with both response slots, actual Fourier backward terminal test f, and C Phi the average over all6 permutations of K(x-z).grad_x Phi(x,y). No source card alone is a proof.

The complete R5 full-pair interface/initial-endpoint proof and THM025 supply the actual inverse and its initial Haar L2 estimate; R8 complete homogeneous particle-domain proof supplies classical off-diagonal and weak Haar derivatives, every true contraction, genuine singular Ito and martingale; R12 full actual-noise proof and THM033 give the scaled whole bracket on bounded rescaled diffusivity; R14 complete lower-drift proof and THM035 give both lower contractions in absolute integrated scale; R16 complete source extension and THM038 give the actual quadratic source through Coulomb. These matching modules have independent accepted gates, while their original issued source histories remain unchanged. R15's literal algebra is a useful consistency check but its d>=4,s<2 theorem is not extended by renaming its card.

## 2. Every range implication

Write p=s+2,a=s/p,theta=1-s/d. Strict sub-Coulomb gives0<s<d-2<d and theta>0. The noise condition says a<theta exactly, since theta-a=[2d-s(s+2)]/[d(s+2)]>0.

It also implies s<d/2. For d=3, strict sub-Coulomb gives s<1<3/2. For d>=4, if s>=d/2 then s(s+2)>=d(d+4)/4>=2d, contrary to strictness. Thus every source and initial L2 exponent required below is in its admitted range.

The R14 admissible interval is nonempty. It suffices to show3s<2d-2, since 0<s<d-2 handles1<d-s-1 and s/2<s+1, while s<d/2 gives remaining d/2 bounds. For d=3, s<1 implies3s<3<4. For d>=4, at s0=2(d-1)/3 one has s0(s0+2)-2d=(4d^2-14d-8)/9=2(2d+1)(d-4)/9>=0. The increasing function s(s+2) and its strict upper bound2d imply s<s0. Therefore q_lo=max(1,s/2)<q_hi=min(d/2,d-s-1,s+1). The midpoint q is fixed, satisfies1<q<d/2,q<=s+1,q<d-s-1 and q>s/2. Hence kappa=(2q-s)/(2p)>0, precisely the R14 exponent. No endpoint equality is included.

At positive finite criticality, nu_N=lambda_N^-1 N^-theta tends to zero, beta_N tends to infinity and b_N=1 eventually. Moreover

    chi_N=nu_N N^(2/p)
         =lambda_N^-1 N^[s(s+2-d)/(d(s+2))] ->0,

because s+2-d<0. The entire eventual sequence is in fixed bounded nu and chi intervals, using only finite eventual upper/lower bounds on lambda_N. All R5/R8/R12/R14/R16 hypotheses therefore hold on one common tail. Finite initial terms do not affect the asserted limit; the quantitative estimate is expressly eventual.

## 3. Genuine actual identity, including backgrounds

Let g_t(x)=integral B Phi_t(x,y)dy and c_t=integral B Phi_t dxdy, B Phi=K(x-y).(grad_x-grad_y)Phi. R8 proves for the actual representatives

    dP_N[Phi_t]=[-P_N[J_t]+U3[C Phi_t]
                 +N^-1 rho_t[g_t]+(2N)^-1 c_t]dt+dM2_t,
    grad_i P_N[Phi_t]=N^-2 sum_(j!=i)grad_x Phi_t(X_i,X_j)
                       -N^-1 integral grad_x Phi_t(X_i,y)dy,
    M2_t=sqrt(2nu_N) sum_i integral_0^t grad_i P_N[Phi_r] dW_i.

The full corrected U3 includes every Haar background. Its raw all-distinct term may be empty at N=2 while backgrounds remain. The factor1/2, scalar lower term and both response slots are unchanged. The inverse is locally C1 in time/C2 off diagonal, globally H1 and W2,1 in Haar space with actual weak contraction derivatives. Full R8 fixes the structurally grouped B Phi integrability rather than a nonintegrable crude force product. For the cubic raw term, |K(x-z)| times the allowed grad-Phi weight is integrable in its two separate relative variables; backgrounds at distinct empirical locations have separated local singularities. Its fixed-N density bound transfers these Haar integrals and gradient squares to actual time integrals. Collision stops exhaust each actual path, drift passes in L1 and martingale in L2. Thus the identity and M2 are genuine for each N on the admitted tail; the density bound is never used as a uniform-N estimate.

Terminal Phi_T=0 and M2_0=0 give

    integral_0^T U3[C Phi_t]dt
      =integral_0^T P_N[J_t]dt-P_N[Phi_0]-M2_T
        -integral_0^T [N^-1 rho_t[g_t]+(2N)^-1 c_t]dt.          (3.1)

This sign follows by integrating dP from P_0 to0. No absolute instantaneous cubic estimate is needed or inferred.

## 4. Four uniform quantitative terms

R16 gives sup_t E|P_N[J_t]|<=C N^-theta for the exact same backward test, uniformly over the eventual bounded-noise interval. Tonelli and sigma_N=sqrt(N) yield scaled first term<=C N^(1/2-theta)=C N^(s/d-1/2).

Since2s<d, R5 gives sup_t||Phi_t||_(L2(Haar^2))^2<=C. For a symmetric Haar L2 kernel F, let m=integral F,q0(x)=integral F(x,y)dy-m,Fcirc=F-q0(x)-q0(y)-m. Literal deleted-label expansion gives P_N[F]=D2[Fcirc]/2-eta_N[q0]/N-m/(2N). Initial iid Haar and zero rows give

    E|P_N[F]|^2=(N-1)||Fcirc||2^2/(2N^3)
                   +||q0||2^2/N^3+m^2/(4N^2).

The components are orthogonal in Haar L2, with ||F||2^2=||Fcirc||2^2+2||q0||2^2+m^2. Thus N E|P_N[Phi_0]|^2<=C/N and sigma_N E|P_N[Phi_0]|<=C N^-1/2. Approximate in Haar L2 if needed; no positive-time iid law is used.

R12 supplies Q_N=sigma_N^2 E<M2>_T<=C b_N N^a(N^-theta+nu_N)<=C N^(a-theta). Its complete whole bracket includes every empirical/background cross term and both response slots; no pair-force-square bound is extracted. The true martingale and Ito isometry imply sigma_N E|M2_T|<=sqrt(Q_N)<=C N^((a-theta)/2).

R14, with the exact midpoint q checked above, supplies

    sigma_N E integral_0^T |N^-1 rho_t[g_t]+(2N)^-1 c_t|dt
       <=C sqrt(b_N) N^-kappa=C N^-kappa.

This controls the combined exact lower functional and retains both summands. No cancellation between them is postulated. Its actual representative matches R8, and its absolute-integral bound is stronger than the absolute value after integration needed in(3.1).

Triangle inequality in(3.1) proves the exact four-term THM043 rate. All exponents are negative by Section2. Every constant is controlled by fixed data and eventual bounds on lambda_N. The exact negation is thereby excluded conditional on the complete source modules and pending the new independent gates. This is an additional range extension of the integrated cubic theorem, not the entire higher hierarchy or an instantaneous U3 estimate. The limiting-law modules R17–R19 and path module R20 are nondependencies.

## 5. Next independent obligations

The complete proof attempt has no unresolved analytic line conditional on its exact source modules. The accompanying complete-source dossier, fresh exact coefficient/exponent diagnostic and exposure/archive seals freeze this attempt. Fresh whole statement-only reconstruction and separate hostile review remain required. Root is exposed to all earlier campaign context, and no same-context self-check can replace those gates. Earlier theorem cards and scientific mission remain unchanged.

## 6. Reproducible self-check and limitations

The accompanying new standard-library exact diagnostic passes 12,531 assertions in 18 categories with four mutation families. It tests all range implications on an exact rational grid, the factorization used for the lower-drift interval, every scaling exponent, literal deleted-label and initial iid second-moment identities on a three-point probability space, and the scalar lower coefficient. These finite checks do not prove singular analytic bounds or certify the source modules. No randomness, tolerance, dependency installation, external citation or novelty claim is involved. The README gives exact reproduction commands; the exposure record discloses prior context.
