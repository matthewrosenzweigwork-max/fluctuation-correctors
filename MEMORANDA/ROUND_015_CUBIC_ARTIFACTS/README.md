# TASK-075 — sealed critical cubic construction

This packet contains a complete candidate proof of the exact THM-036 integrated cubic assertion throughout its frozen range. The proof is conditional on the complete permitted earlier modules and is self-checked. It is not independently certified, and root alone integrates it.

The new estimate controls the absolute quadratic source under the actual singular particle law by the sharp energy scale. Its proof combines a positive low-heat remainder, a smaller-cutoff Fourier commutator estimate, and a separate high-frequency tail of the fixed smooth terminal test. The literal R8 identity then controls the integrated cubic, retaining the genuine initial endpoint, both lower contractions, and full martingale. The instantaneous absolute cubic integral is not claimed.

Files:

- `../ROUND_015_CRITICAL_CUBIC_RESIDUAL.md`: complete assertion/negation, conditional-source preflight, proof, regime calculation, singular-domain check, adversarial self-review, and dispositions.
- `round015_cubic_exact_checks.py`: newly written standard-library exact diagnostic, independent at the algebraic level within this constructor context.
- `round015_cubic_exact_results.json`: exact checker result and evidence limits.
- `EXPOSURE.md`: ambient context and complete construction-stage steering disclosure.
- `INPUT_SHA256SUMS.txt`: the exact 27-file permitted input manifest, copied without modification.
- `VERIFICATION.md`: pre-seal checks, scope, and branch/base facts.
- `OUTPUT_SHA256SUMS.txt`: hashes of all issued proof/checker/report files; excludes itself.
- The timestamped ZIP, `ARCHIVE_VERIFICATION.json`, and `ARCHIVE_SHA256SUMS.txt`: the immutable handoff and its separate member-by-member verification and outer seal.

The ZIP preserves repository-relative paths and contains precisely the 27 allowed inputs, their original assigned manifest, the named outputs, and the output manifest. It contains no other inherited worktree files. Extract into a fresh directory to reproduce the restricted dossier. Run the checker only in that disposable extracted copy: it writes its deterministic JSON result. Do not rewrite the issued packet.

Reproduction command in a fresh extracted copy:

```text
python3 MEMORANDA/ROUND_015_CUBIC_ARTIFACTS/round015_cubic_exact_checks.py
```

The checker uses rational Fourier coefficients, literal subset/distinct-label sums, a direct N-particle generator, exact initial second moments, an exact positive-kernel decomposition, and solvable high-frequency cancellation. The result records its exact count. There is no floating-point tolerance, random seed, package installation, prior checker, or numerical law simulation.

Worktree: `/Users/matthewrosenzweig/.codex/worktrees/hocf-r015-cubic`.
Branch: `codex/hocf-r015-cubic`.
Base: `1df1805ed7cd8f7c295945f99273c8ffdb598e74`.

All input bytes and the assigned base/branch were verified. No tracked base file changed; only the permitted copied task/theorem/manifest and this lane's assigned outputs appear in the isolated worktree status. No canonical file, state ledger, prior issued report, or other worker output was edited. No commit, push, installation, external search, or child agent was used.

The next gate is a fresh statement-only reconstruction and hostile audit, arranged by root. Any correction after issue must be a new superseding artifact, with a new seal. The full campaign, broader regimes, hierarchy closure, and a fluctuation law remain separate.
