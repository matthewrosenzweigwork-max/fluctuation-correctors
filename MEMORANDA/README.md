# Memoranda

Maintain a cumulative mathematical memorandum in amsart TeX. Use versioned filenames and a `CURRENT` pointer. Every theorem/lemma must expose status and dependencies until independently audited.
