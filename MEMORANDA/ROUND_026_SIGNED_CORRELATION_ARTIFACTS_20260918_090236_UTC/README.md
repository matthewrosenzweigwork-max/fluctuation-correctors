# Round 026 signed-correlation construction packet

The original critical signed-correlation target remains OPEN. This
SELF-CHECKED packet proves a bounded new obstruction to the attempted
conditional-response sensitivity mechanism, plus an exact nonnegative
conditional-mean/noise-response decomposition. Read REPORT.md and the
complete CLAIM_CARD.md before treating any conclusion as a premise.

The actual finite-N backward semigroup of a smooth empirical cosine
has a nonzero angular projection at an initial pair collision. Hence
its Haar fourth-gradient control, and cutoff-uniform global C1 control,
fail. Actual L2 response estimates remain possible and unproved here.
No admitted critical positive-limsup witness or source decay is claimed.

- REPORT.md is the complete mathematical memorandum.
- CLAIM_CARD.md states the entire new conjunction and its negation.
- EXPOSURE_AND_SOURCES.md records exact reading and isolation.
- INPUTS contains all 17 complete source files; INPUT_SHA256SUMS.txt is
  the byte-exact supplied manifest.
- scripts/diagnostic.py is a fresh standard-library self-check.
- RESULTS contains its passing baseline and every nonzero mutation control.
- RUN_HISTORY.md records commands, outcomes, limitations, and issuance.
- MEMBERS.txt lists exactly every packet regular file, including itself
  and PAYLOAD_SHA256SUMS.txt.
- PAYLOAD_SHA256SUMS.txt hashes every other packet file. Its own digest,
  the sibling archive digest, and memorandum digest are in the external
  packet seal. This avoids a circular self-hash.
- scripts/verify_packet.py is a portable read-only verifier.

From any directory, run Python 3 on the verifier with this packet
directory as its positional argument. Optionally pass --archive with
the sibling tar.gz path and --seal with the sibling PACKET_SEAL.json.
The verifier never extracts or writes files, installs dependencies,
invokes Git, or accesses original repository inputs. It verifies copied
sources, exact payload membership, hashes, readonly modes, safe archive
members, and byte equality between the archive and directory.

Issued files are immutable/read-only. Any correction requires a new
named issuance. The sibling seal and archive are handoff artifacts, not
independent mathematical certification. Root alone assigns campaign
identifiers and review gates. No TeX/PDF is included: the mathematical
deliverable is the Markdown report, and no TeX build is claimed.
