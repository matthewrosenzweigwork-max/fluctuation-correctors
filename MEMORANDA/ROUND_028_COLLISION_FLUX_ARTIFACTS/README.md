# TASK121 — issued collision-flux construction packet

This is the ordinary Astra Max construction and falsification packet for the whole frozen THM053(A)–(B), 2026-09-18 UTC. Mathematical disposition: **PROVED_CANDIDATE**. Audit disposition: **SELF_CHECKED ONLY**. The root owns integration and must obtain fresh whole-claim independent audits before promotion. No theorem about the original critical cancellation is asserted.

The complete proof is `ROUND_028_FIRST_COLLISION_FLUX.md`. It reconstructs the needed killed-duality survivor identity from the supplied R27 method, proves the first-power drift action and actual terminal limit, justifies stopped Itô with true martingales, identifies the entire joint time/configuration measure including all singular coefficients, and only then proves unique binary collision and the marked independence conclusions. `CLAIM_AND_NEGATION.md` maps every frozen clause to the proof and retains the exact negation. `FAILED_ROUTES_AND_FALSIFICATION.md` records the rejected shortcuts and the separate-form diagnostic route.

All twelve frozen inputs are copied under `INPUTS/` with original relative paths. `INPUT_SHA256SUMS.txt` is the original task manifest; `INPUT_PREFLIGHT.json` records successful original and copy verification. `EXPOSURE_READ_HISTORY.md` lists the complete input exposure, reading recovery, mathematical source limits, and operational history. No original input, canonical state, current audit, or other route was modified.

`diagnostics.py` is original standard-library code written in this constructor context. `DIAGNOSTICS_STDOUT.json` retains every exact input and result; `DIAGNOSTICS_STDERR.txt` is empty. The actual run returned exit 0 with **1,480/1,480 exact checks passed and 11/11 nonzero mutations detected**. There is no random seed, floating-point tolerance, numerical approximation of pi, external dependency or numerical SDE. The local polynomial and deterministic Euclidean examples are coefficient diagnostics outside the frozen probabilistic hypotheses, explicitly labeled in code, output and proof.

Run from any copy of this packet, without installation:

```text
python3 -B diagnostics.py
python3 -B verifier.py
```

The first command prints the exact diagnostic summary. The second verifies the complete regular-file inventory and every payload digest, all twelve frozen copies, read-only issuance, the proof copy, deterministic diagnostic reproduction, and in-memory rejection tests for unsafe names, duplicate entries and nonregular archive members. It performs no file writes. For the issued archive and the separately delivered memorandum, optional explicit arguments also verify byte-for-byte equality:

```text
python3 -B verifier.py --archive ../ROUND_028_COLLISION_FLUX_ARTIFACTS_20260918_101125_UTC.tar.gz --memo ../ROUND_028_FIRST_COLLISION_FLUX.md
```

The archive contains only regular files, with safe relative names below one packet prefix; it has no directory, link, device, or other special members. The named sibling `ROUND_028_COLLISION_FLUX_ARTIFACTS_20260918_101125_UTC_ARCHIVE_MEMBERS.json` inventories every archive member, including the payload manifest itself. The named sibling `ROUND_028_COLLISION_FLUX_ARTIFACTS_20260918_101125_UTC_SEAL.json` records the archive digest, manifest digest, complete archive-inventory digest, separate memorandum digest, final verification output and issuance timestamp. All issued files are read-only, with read-only packet directories. The final verifier output is in the external seal to avoid circularly hashing an internal report of its own digest.

`CONTENT_SHA256SUMS.txt` inventories and hashes every regular payload file except itself; the verifier requires that to be the exact complete file set. Its own bytes are covered by the full archive inventory and archive digest in the external seal. This explicit trust boundary avoids pretending to give a self-hash. Integrity verification does not certify mathematical truth, and a mutable local copy of a seal cannot provide external authentication by itself.

No TeX source was created, so no TeX compilation is claimed. The task required a Markdown proof and evidence packet. No children, dependencies, commits, pushes, remotes, external browsing, memory-file reads, other-worktree reads or canonical edits occurred. Once issued, this packet is not edited; any correction must receive a distinct report and seal.
