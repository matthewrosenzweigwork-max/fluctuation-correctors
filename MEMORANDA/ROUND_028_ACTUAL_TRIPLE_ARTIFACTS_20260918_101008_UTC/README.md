# R28 actual three-label construction packet

TASK119; ordinary construction, SELF_CHECKED only. The original THM046
critical source assertion and its admitted positive-limsup negation remain
unproved. No independent certification is claimed.

`REPORT.md` is the full issued proof attempt. Its new conjunction is:

- uniform fourth moments of the actual scaled empirical modes, integrated
  sources and endpoint defects, with an explicit polynomial mode weight;
- quantitative vanishing of the exact three-label contribution tested
  against a heat-smoothed observable at a shrinking scale;
- an exact transfer, with a vanishing error, to a three-label integral of
  the discarded raw force scales, with the row term accounted for.

The first unproved line is report equation (9.3). Actual singular dynamics,
iid Haar preparation, fixed external mode/horizon, original time weights,
centering, all finite-label coefficients and physical cutoff order remain.

`INPUTS/` contains all 21 exact permitted inputs. `INPUT_SHA256SUMS.txt`
is the supplied manifest; `INPUT_CHECK.json` records the verified bytes.
`EXPOSURE_AND_SOURCES.md` distinguishes full and partial displayed reads.
`RUN_HISTORY.md` records successful operations and the failed nonexistent
read. The packet does not use a current R27 route, concurrent R28 route,
canonical state/history, memory files, another worktree, earlier code,
unlisted linked sources, or external literature.

`diagnostic.py` uses Python's standard library and exact rational/Gaussian
rational arithmetic. The saved baseline passes 5,695 assertions across
31 categories. `OUTPUTS/MUTATION_RUNS.json` and its named stdout/stderr
and run records preserve 17 actual nonzero mathematical mutation runs.
The test's finite groups and finite Fourier kernels are supporting algebra,
not admitted substitutes for the actual particle law or a continuum proof.

The issued directory, memorandum, archive and seals are read-only. Every
archive entry is a regular file under one safe relative root. The sibling
`.MEMBERS.json` covers every directory/archive regular member, including
both internal control inventories. The sibling `.SHA256SUMS.txt` seals
the memorandum, archive, external member inventory and final verification
receipt without claiming a
self-referential digest. The sibling `.VERIFICATION.json` records final
verification and read-only guard controls.

From any working directory, with the directory and named sibling archive
and member inventory kept together, run:

```text
python3 /path/to/ROUND_028_ACTUAL_TRIPLE_ARTIFACTS_20260918_101008_UTC/verify.py --run-diagnostic
```

The verifier only reads. It checks exact member sets, all digests, all 21
source hashes, safe regular archive members, directory/archive equality,
read-only member modes, and fresh exact diagnostic equality. It never
extracts the archive. Optional `--archive` and `--members` arguments allow
explicit sibling locations. The diagnostic itself is also read-only.
No dependency installation, TeX generation, source edit, commit, push or
remote change is part of this packet.

Corrections after issuance require a new artifact, not changes here.
