# Execution and proof-attempt history

This history records execution evidence and proof disposition, not private
scratch reasoning. The complete argument, failed inequality and next open
line are in REPORT.md. All times use UTC; the artifact was initialized
with the observed timestamp 20260918_101008_UTC.

1. Read the assigned TASK119 card and its input manifest first. Observed
   working directory exactly matches the isolated assignment.
2. Ran the manifest check over all 21 frozen rows. Every row returned OK,
   exit code 0, before mathematics. Independently read Git root, branch and
   short status, plus input line counts. Root/branch match; pre-existing
   untracked supplied R26 reports/exposures/cards, TASK119, manifest and the
   explicitly permitted THM050 comparison were preserved. No history read.
3. Read the full instructions/model and the allowed theorem/audit/exposure
   set. One combined tool display truncated the theorem-card region. Read
   all five theorem cards and the R25 receipt again in one dedicated call.
   Full R26 reports were separately displayed; full R25 was displayed in
   consecutive ranges 1-240,241-480,481-720.
4. Read the specified historical mechanism ranges recorded in
   EXPOSURE_AND_SOURCES.md. One command attempted
   `sed -n '1,155p' MEMORANDA/ROUND_001_ALGRA.md` and returned exit code 1:
   `sed: MEMORANDA/ROUND_001_ALGRA.md: No such file or directory`.
   Corrected the typo in the next read to the exact allowlisted
   `ROUND_001_ALGEBRA.md`; no bytes were obtained from the misspelled path.
5. Read Git HEAD, which is ef438b612f732d21af1fa1756dd7559ef63c6d61, and
   Python version 3.9.6. No model-setting UI or external source was read.
6. Derived the new fourth-moment graph identity, restricted fourth-moment
   energy-martingale estimate, actual mode/source fourth moments, and the
   shrinking heat-observable triple estimate. The attempted absolute
   cutoff removal yields the nonvanishing bound (9.2); it is not promoted.
   Independently tested the direct untruncated energy-fourth-moment route:
   it fails already at an admitted initial close pair. The repaired good/
   bad event proof controls values but does not settle the force-tail gate.
7. Wrote the assigned memorandum and initialized the unique packet. Copied
   all 21 input bytes, rechecking all digests during copying. Stored the
   original manifest and input sizes/digests. No canonical/source edit.
8. Wrote the new diagnostic and read-only verifier. First baseline command
   was `python3 diagnostic.py`; return code 0. Its exact saved record is
   OUTPUTS/baseline.run.json, stdout and stderr alongside it. The first
   shell response yielded while that short process ran; a later read of
   its completed record confirmed the result. No mathematical baseline
   failure or diagnostic code repair occurred.
9. Baseline: 5,695 exact assertions, 31 categories, 17 distinct nonzero
   mutation witnesses. Executed all 17 named `--mutant` runs separately,
   at most four independent subprocesses concurrently. Each returned code
   1 with the expected `MATHEMATICAL_MUTATION_CAUGHT` witness. Every
   stdout, stderr, return code, duration and program hash is retained under
   OUTPUTS/mutations, with aggregate OUTPUTS/MUTATION_RUNS.json. These are
   intentional rejected controls, not suppressed failures.
10. During self-review clarified that the fourth-energy identity is on a
    compact abelian Haar group; clarified that separate smallness is proved
    only for the smoothed A3r/B3r, not the original singular correlations.
    Chose the final r_N exponent 1/24 to balance the row-removal error;
    the initially communicated 1/44 remains a valid special case for the
    centered smoothed term. No theorem scope or original target changed.
11. Root then sent a definition clarification asking that the additive
    group convention in Section 4 be explicitly abelian. The immediately
    preceding self-review patch already contained this restriction; no
    new proof or cross-route narrative was supplied. While recording that
    message, one two-file patch was rejected atomically because its second
    context line was absent from RUN_HISTORY. A read confirmed neither file
    changed, and a corrected patch recorded the message and this failure.
    No mathematical code, claim or immutable evidence was changed by the
    rejected patch.
12. Prepare the immutable report copy, exact inventories and archive, then
    seal every issued file read-only. The final archive/directory verifier,
    fresh diagnostic replay and three deliberately rejected in-memory guard
    controls are recorded in the named sibling VERIFICATION receipt, since
    inserting that final receipt into its own sealed inputs would be
    self-referential. Guard controls change only in-memory expected metadata
    or test names; they do not alter packet bytes or extract an archive.

Final mathematical status: the complete new bounded conjunction is a
SELF_CHECKED candidate. THM046 and the exact remaining report line (9.3)
are OPEN, with no admitted witness. Root owns all subsequent integration,
canonical updates, new identifiers, source equality and independent gates.
No child, installation, source/canonical edit, commit, push, remote change,
external contact or publication occurred.
