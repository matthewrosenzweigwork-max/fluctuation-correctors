# R22 root distribution-path packet

SELF_CHECKED exposed construction, conditional on the full supplied source chain, especially pending R20; both new independent whole gates remain open. Sixteen exact input copies are under INPUTS. Adjacent proof is reproduced exactly. Source/exposure records delimit all imports and root contamination.

From repository root:

    python3 MEMORANDA/ROUND_022_DISTRIBUTION_PATH_ARTIFACTS/round022_hilbert_checks.py --output /absolute/new/output.json
    python3 MEMORANDA/ROUND_022_DISTRIBUTION_PATH_ARTIFACTS/verify_packet.py

Expected47437 exact assertions,19 categories,12 mutation witnesses. Use a new output outside this immutable packet. No randomness, tolerance or dependencies. Finite tests support analytic proof and do not replace independent source/path-limit review.

The adapted verifier checks all input/output bytes, exact payload membership, adjacent proof identity, code/result correspondence, complete safe regular ZIP membership and exact archive bytes without extraction. OUTER_SEAL.json hashes the output manifest and archive outside the ZIP. Read-only modes supplement hashes; new findings require new artifacts. No TeX/PDF is claimed in this packet.
