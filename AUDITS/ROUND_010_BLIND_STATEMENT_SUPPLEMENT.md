# Round010 complete statement-only supplement for fresh reconstruction

This file supplies missing notation and exact subordinate claims referenced by the frozen THM030/031 cards. It contains statements, not candidate derivations. Every claim is to be reconstructed or rejected. The original cards remain unchanged. No whole-card independent pass exists at this issue.

Use both cards' common homogeneous actual iid-Haar model and genuine full pair inverse. Haar measure has mass1. H_N=N^-1 sum_(i<j)g. b_N=min(1/nu,1),sigma_N^2=N b_N. Zero-noise noise functionals are zero separately. Constants independent of N,nu,time unless specifically noted. Domain/Haar source theorems are explicit conditional premises as issued.

For THM030 set p=s+2,a=s/p. There exists a fixed C0 for H_N^epsilon>=-C0 N^a for every heat convolution epsilon>=0, the epsilon0 version on collision-free configurations. For2<=k<=N let e_k=min(2,sqrt(2C0(k-1)N^a/(nu(N-1)))). Every bounded k-test Z has actual-minus-Haar expectation at most ||Z||infinity e_k in absolute value; e_k<=2sqrt(C0(k-1)/nu)N^-1/p. At N2 no triple object. For everyL>0 the card's clipped actual martingale Q^L obeys its exact bound. Sufficient thresholds: L_N->infinity and L_N^2 N^-1/p->0; or, for a specified positive-noise sequence, b_N sqrt(nu_N)L_N^2 N^-1/p->0 instead. With those thresholds, full Q_N->0 iff its complementary expected scaled bracket T_N(L_N)->0.

For any vector kernelG, row A=integralG,H=G-A, define v_i=N^-2[sum_(j!=i)H_ij-A_i]. Under an exchangeable law with integrable products, E sum_i|v_i|^2=N^-3[(N-1)E|H12|^2+(N-1)(N-2)E H12.H13-2(N-1)E H12.A1+E|A1|^2]. Triple absent atN2. Under Haar the exact result is (N-1)/N^3||G||2^2-(N-2)/N^3||A||2^2. Apply each formula to its own full,clipped or complementary field; no gradient-of-new-potential or orthogonality assumption for clipping. Haar reference tail<=C_E b_N N^-2/p; its available actual full-density comparison is at most exp(kappa(N-1)T) times that bound.

For s>2, every1<r<s/2 is also below d/2. Let C_(N,r) be the supplied finite-N constant |G_t|<=C_(N,r)w_r. For w_r^(s/r)<=C w_s and the uniform actual/Haar w_s moment, the tail obeys T_N(L)<=8nu b_N T C_(s,r) C_(N,r)^(s/r)L^(2-s/r). Thus nu_N b_N C_(N,r)^(s/r)N^(-(s/r-2)/(4p))->0 is sufficient for the explicit threshold, but is not established by finiteness of C_(N,r). For0<s<=2, this particular larger-than-quadratic-moment method has no admittedr withs/r>2; this is only a limitation of that argument.

For THM031 set theta=1-s/d. The sharp heat-integral floorH_N>=-C N^(s/d) yields E|eta_hat_N(t,k)|^2<=min(1,CN^-theta |k|^(d-s)) for all nonzero integerk. Ent(F_N)<=C beta N^(s/d)=CN lambda_N; fixedk marginal entropy<=Ck lambda_N for k<=N/2. Therefore fixed marginals converge in total variation in full subcriticality, with no critical conclusion. Expectedpair|g|<=2|inf g|. Full force B_i=N^-1 sumK_ij satisfies E integral_0^T|B_i|^2<=C N^-theta+nu kappa T. The expected maximal squared torus distance between Xi(t) and Xi(0)+sqrt(2nu)Wi(t), and between Xi(t) andXi(0), is <=C_T(N^-theta+nu). At criticality this isO(N^-theta). No individual pair-force square follows.

The true fullPhi has uniform HaarL1norm; each fixed common-translation derivative has a uniform HaarL1norm. Its projectionq_t(x)=integralPhi_t(x,y)dy has uniformly bounded Cmnorm for everyfixedm. IntegralPhi=0 and actualE P_N[Phi_t]=0, with no concentration conclusion. Psi_delta=exp(delta Delta_x)exp(delta Delta_y)Phi,0<delta<=1. Its first-slot gradientG_delta has y-Fourier coefficient bounded by C delta^(-(d+1)/2)exp(-4pi^2delta|k|^2), uniformly inx. Let r_*=(5d-s+2)/2. Then Q_N[Psi_delta]<=Cnu b_N[N^-theta delta^-r_*+N^-2 delta^(-(2d+1))]<=C N^-theta delta^-r_*. Withdelta_N=N^(-(d-s)/(d(5d-s+2))), this isO(N^-theta/2). FullQ_N tends to0 iffQ_N[Phi-Psi_delta_N] tends to0, equivalentlylim_(delta->0)limsup_(N->infinity)Q_N[Phi-Psi_delta]=0. AtfixedN the smoothing tail tends to0, which by itself doesnot interchange limits.

For the card's smooth real row-centered vector probe, initial triple covariance iszero and its derivative is exactly-(2/N) integral_x sum_(k!=0)d_k|H_hat_0(x,k)|^2. Coulomb d_k=c_d and the formula is-(2c_d/N)||H_0||2^2. N>=3 only. No uniform singular Taylor remainder or later sign. Finally, conditional on integratedHaarenergy, Q_N^[0,tau][Phi]<=C b_N exp(kappa(N-1)tau)N^(a-1). For0<zeta<1-a and positiveadmissiblekappa,tau_N=min(T,zeta logN/(kappa(N-1))), theboundisC b_N N^(-(1-a-zeta))->0. No fixed-horizon tail conclusion.

No candidate proof, checker, audit verdict or current R11/R12 result is supplied. Exact scope includes every displayed bound, endpoint and equivalent reduction, but excludes proof of the remaining genuine singular tail, cubic residual, hierarchy or full fluctuation law.
