# AUD077 / TASK122 — immutable joint boundary reconstruction packet

This is the fresh statement-only independent reconstruction of complete
unchanged THM053(A)–(B) and THM055(A)–(E), under the explicit pre-dispatch
joint-scope addendum. Both whole-theorem verdicts are
`ISOLATED_RECONSTRUCTION_PASS`, with no repair of either frozen assertion.
Root comparison, historical source/gate authentication, hostile review and
promotion are separate. The original critical-source limit remains open.

`REPORT.md` is byte-identical to the assigned sibling
`ROUND_028_FLUX_RECONSTRUCTION.md`. `INPUTS/` contains all eighteen exact
allowlisted files; `ADMIN/` includes the two original manifests and full
input/exposure/read/execution records. `CODE/` contains the fresh diagnostic,
logged runner, portable read-only verifier and issuance script. `LOGS/`
contains every original diagnostic run, including all expected failures.
`RESULTS/` contains the baseline, complete mutation witnesses and structural
checks. Structural checks are administrative, not mathematical verification.

The diagnostic passed **7,957 exact assertions in 27 categories**, using only
integer and rational arithmetic. All **fourteen deliberate mutations** failed
with nonzero witnesses and exit 1. It contains no random sampling, approximate
quadrature, SDE simulation, installed dependency or unclaimed source result.
Its scalar/finite-group models are supporting checks, not substitutes for the
continuum proof. There was no unintended diagnostic failure. The source-output
transport truncation, a failed pre-issuance patch and a corrected draft
ancillary qualification at N=2 are explicitly recorded in the history.

Run the portable check from this directory:

```text
python3 CODE/verify.py
```

For the full issued worktree copy, also check the safe archive, outer seal,
assigned report and read-only permissions:

```text
python3 CODE/verify.py --archive ../ROUND_028_FLUX_BLIND_ARTIFACTS_20260918_103751_UTC.tar.gz --seal ../ROUND_028_FLUX_BLIND_ARTIFACTS_20260918_103751_UTC.SEAL.json --report ../ROUND_028_FLUX_RECONSTRUCTION.md --require-readonly
```

The verifier writes nothing, extracts nothing, contacts no service and reads
no repository source beyond this packet and the explicit optional sibling
arguments. It reexecutes the baseline and all fourteen nonzero mutation runs.
After portable extraction, the optional permission check can be omitted if
the extracting tool did not preserve directory modes.

`INVENTORY.json` lists every payload file with exact byte count and SHA-256,
every directory, and the two explicit inventory/manifest control files.
`SHA256SUMS.txt` hashes every file except itself, including the inventory.
The sibling seal binds the manifest, inventory, report and safe archive.
The sibling receipt records the final complete read-only verification, every
reexecuted command and output digest. The sibling `SHA256SUMS.txt` binds that
receipt and the seal as well. The self-exclusion of a checksum list is stated,
never concealed as an omitted payload.

The archive has exactly one safe, unique relative regular-file member for
every packet file, under this packet's name; no links, devices, absolute names,
parent traversal or unlisted members occur. Files are issued with mode 0444
and packet directories with mode 0555. Issued contents must not be amended;
any correction requires a distinct superseding packet and report.

No commit, push, remote operation, canonical edit, child worker, dependency
installation, external/private source or memory-file read occurred. The branch
and base were supplied by root; this isolated lane did not inspect Git state
or history. All writes are the assigned report and this uniquely named packet
with its explicitly named siblings.
