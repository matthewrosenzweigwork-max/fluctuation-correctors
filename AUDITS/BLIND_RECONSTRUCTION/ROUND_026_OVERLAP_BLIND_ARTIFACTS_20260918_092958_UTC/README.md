# AUD073 immutable evidence packet

This is the isolated whole-claim reconstruction for TASK114 / THM051(A)–(D). The mathematical verdict is **RECONSTRUCTED ENTIRE FROZEN CONJUNCTION**. The signed three-distinct-label cancellation and original critical source mission remain open. This is a fresh reconstruction, not a review of the unseen constructor narrative or an independent certification of every theorem mentioned in older permitted sources.

The packet contains the full mathematical reconstruction, all twelve exact frozen input copies, their original digest manifest and verification receipt, an explicit read/exposure record, complete execution history, fresh supporting diagnostics and all saved results. The first diagnostic run passed 134 checks and rejected 34 nonzero mutation instances. It also probes 43,344 mode/cutoff pairs in three finite all-frequency sweeps. Finite algebra uses rational arithmetic; analytic-scale probes use binary64. None simulates or certifies the actual singular law. The proof supplies the actual-law and uniform estimates.

Run the portable read-only verifier from any directory:

```sh
python3 /absolute/path/to/this/packet/verify.py
```

For the whole handoff, supply the named sibling archive, complete member inventory, seal and issued report:

```sh
python3 /absolute/path/to/this/packet/verify.py \
  --archive /absolute/path/to/ROUND_026_OVERLAP_BLIND_ARTIFACTS_20260918_092958_UTC.tar.gz \
  --inventory /absolute/path/to/ROUND_026_OVERLAP_BLIND_ARTIFACTS_20260918_092958_UTC.ARCHIVE_MEMBERS.json \
  --seal /absolute/path/to/ROUND_026_OVERLAP_BLIND_ARTIFACTS_20260918_092958_UTC.SHA256SUMS.txt \
  --report /absolute/path/to/ROUND_026_OVERLAP_RECONSTRUCTION.md
```

The verifier checks all internal file membership and digests, exact input bytes, every saved diagnostic result by rerunning without output-file flags, and optionally the safe archive member list, byte equality to the directory, sibling seal and issued report. It does not extract, install, access the network or write files. The archive contains regular files only: no symlinks, hardlinks, directories, special files, duplicate paths, absolute paths or traversal components. Archive members use mode 0444. Directory files and the issued report are made read-only on issuance; directory modes are 0555.

`REGULAR_MEMBERS.txt` lists every regular file including itself and the payload manifest. `PAYLOAD_SHA256SUMS.txt` hashes every regular file except itself, resolving the unavoidable self-hash cycle. The sibling archive member inventory hashes *all* archived members including both internal controls. The sibling seal hashes the archive, that inventory, the issued report and the verification receipt. The externally reported seal digest closes the final trust boundary. Integrity checks do not prove mathematics or establish that an adversary has not replaced an entire unsigned packet.

Corrections require a distinct superseding artifact. No source/canonical files, ledger, dependency, Git reference or remote were changed. No TeX compilation was needed because no TeX source was edited or mathematical LaTeX placed in the final short handoff. No external reference, private input, child or nonallowlisted source was used.
