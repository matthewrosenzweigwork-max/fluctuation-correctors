# Complete execution and result history — AUD073

All actions took place on 2026-09-18 UTC in the assigned isolated worktree. No shell/Git command was used to infer branch or history. Shell commands had the assigned worktree as their explicit working directory. No dependency was installed and no subprocess was run in another worktree.

1. Read the task and manifest first. Then checked all 12 input hashes; every hash matched. Manifest digest and source hashes are preserved in INPUT_VERIFICATION.json. No failed input check occurred.
2. Read the allowed administrative/model/theorem files and historical ranges exactly as recorded in PROVENANCE.md. Two oversized aggregate outputs were truncated by the tool, explicitly recorded there; no mathematical failure was concealed by truncation. The R6 energy passage was reread in a bounded output. No nonallowlisted reference was followed.
3. Reconstructed the entire claim analytically before receiving any current constructor narrative. Reported the complete potential route to root. No incoming constructor content was used.
4. At 20260918_092958_UTC created this unique packet, copied all twelve input files, rechecked their hashes, copied the manifest, and saved the input verification receipt. Every check passed. No preexisting packet was overwritten.
5. Wrote the complete draft reconstruction to the assigned report path and copied it into the packet. The proof derives all of THM051(A)–(D), retains exact coefficients and does not claim the residual cancellation or THM049's separate source equivalence.
6. Wrote diagnostics.py afresh. The first execution was `python3 <packet>/diagnostics.py --output <packet>/results/diagnostics_run_001.json`. Exit status was zero. Status PASS: 134 checks passed, all 34 mutation instances had nonzero detected residuals; no failing check or undetected mutation. The complete saved output includes each check, all residuals and every finite sweep summary. No failed diagnostic run or abandoned source version preceded this run.
7. The supporting frequency sweep used 14,448 mode/cutoff pairs in each of three fixed mode families: total 43,344. All measurements and maxima are in the saved JSON. They are binary64 probes, not proofs. The exact finite-Fourier and label identities use Fraction-based rational complex arithmetic. Gaussian/layer-cake checks and the excluded stronger-bound mutations are labeled with their limitations. There is no random seed because no randomness was used.
8. Before issuance, refined the report's choice of the cutoff to a universal numerical half, explicitly using the already proved interval up to one half. This removes even an apparent dependence of the critical-window constants on a smaller named cutoff cap. Added the actual diagnostic counts. No asserted theorem, normalization or rate changed. Copied the final report into the packet again.
9. Created the source/exposure record, README and portable read-only verifier. A pre-execution review corrected its before/after digest capture so the no-write check compares to actual pre-subprocess hashes; no failed verifier run preceded this draft correction. The verifier reruns diagnostics without output-file arguments and compares the entire JSON, including every result and mutation. It also checks complete membership, input/payload hashes and optional complete safe archive membership, byte identity and sibling seals. No extraction or writes are needed to verify.
10. Prepared complete internal regular-member and payload-digest inventories. Created the named gzip/tar archive from regular files only, normalized read-only archive modes and deterministic archive timestamps. Created the complete sibling member/digest inventory and initial sibling seal; ran the whole verifier and saved its verification receipt. Added that receipt to the sibling seal and reran the whole verifier read-only. The receipt and externally reported final seal digest provide the exact issued outcome.
11. Issuance makes the report, packet files and named sibling artifacts read-only; packet directories use mode 0555. Every source input remains unchanged. No Git mutation, commit, push, remote action, canonical update or child occurred. Root owns comparison with the separate constructor and hostile axes, integration and any mathematical promotion.

No new failed mathematical candidate or admitted counterexample was found in this lane. The deliberately incorrect mutations are all preserved, not presented as theorem counterexamples. The genuine open item after this report is the same fixed-data signed three-label cancellation. Corrections after issuance require distinct superseding names rather than rewriting these bytes.
