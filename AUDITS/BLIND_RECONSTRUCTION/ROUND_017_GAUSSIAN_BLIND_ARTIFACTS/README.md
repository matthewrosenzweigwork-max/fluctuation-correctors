# AUD053 / TASK-082 sealed reconstruction packet

This packet contains the full fresh reconstruction of THM-039, conditional only on the explicitly identified complete R16 actual-source premise. The report proves the finite-N first-order identity and singular passage, exact centering, quantitative approximation, and full joint Gaussian weak convergence with degenerate covariance. R16's separate gate is not presumed passed. No admitted counterexample was found.

The main report is `ROUND_017_FINITE_DIMENSIONAL_RECONSTRUCTION.md`, a byte-identical packet copy of the sibling issued report. `INPUTS/` contains all eleven frozen source byte strings. `INPUT_SHA256SUMS.txt` reproduces the supplied allowlist; `INPUT_VERIFICATION.json` records initial root/copy verification. `EXPOSURE.md` records ambient exposure and isolation. `round017_gaussian_blind_exact.py` is new independent diagnostic code, and its results JSON records PASS with 2,447 exact assertions in 31 categories. The tests are finite smooth algebraic probes, not a numerical proof of the singular result. `OUTPUT_SHA256SUMS.txt` covers all payload files except itself; the archive seal covers that manifest too.

The branch is `codex/hocf-r017-gaussian-blind`, from published R13 `072cab684b9ce41855ead6c48f435c8fc184ec35`. The worktree is `/Users/matthewrosenzweig/.codex/worktrees/hocf-r017-gaussian-blind`. No prior checker was reused. No files outside the task allowlist were read for mathematical evidence. No children, commits, pushes, installs, external searches or canonical edits occurred. This report is sealed before candidate comparison; root alone integrates and assigns canonical gate status.

Run the read-only packet verifier from any directory:

```text
python3 /absolute/path/to/ROUND_017_GAUSSIAN_BLIND_ARTIFACTS/verify_packet.py
```

Add `--archive /absolute/path/to/the-issued.tar.gz --expected-sha256 THE_ISSUED_DIGEST` to check its byte digest, exact member set, regular-file-only membership, absence of traversal/links, and byte identity with every on-disk packet member. Add `--worktree /Users/matthewrosenzweig/.codex/worktrees/hocf-r017-gaussian-blind` to recheck all eleven worktree input copies as well.

To rerun the exact diagnostic without modifying sealed outputs, choose a fresh output path outside this packet:

```text
python3 /absolute/path/to/ROUND_017_GAUSSIAN_BLIND_ARTIFACTS/round017_gaussian_blind_exact.py --output /absolute/path/to/new-results.json
```

The issued output is deterministic: exact rational and Gaussian-rational arithmetic, no randomness, no tolerance, and no dependency installation. The script digest in results must match the sealed script. Its normalized derivative omits one factor of `2*pi`; restoring `(2*pi)^2` recovers the physical generator/source/bracket units. Rational covariance multipliers are declared algebraic semigroup probes.

Packet files and the issued report are made read-only before archive creation. The sibling timestamped archive, digest, verification JSON, and final handoff manifest are also made read-only. The final manifest is the outer checksum anchor and cannot hash itself. Corrections require a separately issued superseding report or addendum, not edits to these sealed bytes.

The bounded remaining action is root comparison, verification that the separately reviewed source gate covers the precise premise, required hostile review, and canonical integration. No path-space or wider-law result is claimed. No TeX/PDF is part of this Markdown-and-diagnostic task; no mathematical LaTeX appears in the final handoff message.
