# TASK066 — sealed fresh THM033 reconstruction

This bundle supplies a complete conditional reconstruction of the frozen quantitative gradient, actual-law noise, leading/cross bracket and strict-range claims. It assigns no theorem or audit pass. The exact source premises, their retained limitations, first new estimates, exclusion of the Coulomb endpoint, and separate coefficient diagnostics are in the report.

No withheld current constructor narrative, diagnostics, R11 output, current audit/state/history, memory file, or earlier checker was read. The 23-file dossier was copied and hash-verified first. Ambient automatic memory-summary exposure is recorded in EXPOSURE.md. No current mathematical comparison occurred before sealing.

Files:

- `ROUND_012_ACTUAL_NOISE_RECONSTRUCTION.md`: full mathematical report and conditional disposition.
- `round012_noise_exact.py`: newly written standard-library exact checker.
- `round012_noise_exact_result.json`: successful result, 2,750 exact assertions, and all 23 source hashes.
- `EXPOSURE.md`: input/context restrictions and worktree provenance.
- `INPUT_SHA256SUMS.txt`: immutable copy of the exact root 23-input manifest.
- `SEAL_METADATA.json`: issue time, base commit, lane, and seal scope.
- `OUTPUT_SHA256SUMS.txt`: output hashes, relative to this directory, excluding itself.
- `ROUND_012_ACTUAL_NOISE_RECONSTRUCTION_20260918.tar.gz`: exact permitted inputs plus the sealed reconstruction output files, with worktree-relative member paths; no inherited non-allowlisted file is included.
- `ARCHIVE_SHA256SUMS.txt`: external archive seal; a manifest is never asked to hash itself.

Executed verification from the isolated worktree:

```text
python3 AUDITS/BLIND_RECONSTRUCTION/round012_noise_exact.py
```

Outcome: 2,750 exact checks passed. This checks algebra and exponent arithmetic only. It does not replace the proof or certify any prior module.

For independent read-only checking, verify INPUT_SHA256SUMS.txt relative to the worktree/archive root, OUTPUT_SHA256SUMS.txt relative to this directory, and ARCHIVE_SHA256SUMS.txt relative to this directory. The checker writes its JSON result; run it in a separate extracted copy if retaining issuance bytes matters. Its result is deterministic and has no timestamp.

The archive includes the exact 23 input byte strings, the copied root input manifest, and the seven sealed output files plus output manifest. Archive inspection verifies member names and hashes without invoking extraction. No TeX was created or compiled; the requested mathematical deliverable is Markdown and the final handoff contains no mathematical LaTeX.

The next authorized root action is to verify these seals and compare with the withheld current construction. A separate fresh hostile review remains required. Any correction after issuance must be a separate superseding report; do not edit this report or regenerate its issued result in place.
