# AUD059 exposure and write record

## Isolation

The first repository reads were exactly TASKS/ACTIVE/TASK-091_ROUND020_CONTINUOUS_PATH_BLIND.md and AUDITS/ROUND_020_CONTINUOUS_PATH_BLIND_INPUT_SHA256SUMS.txt in the root repository. The assigned branch/worktree was then created from e75f8b780682a7e9fb0715b5e8b5873be684a2e8. Only the fourteen prescribed files and the controlling manifest were copied into it; hashes were checked before and after copying.

There was no general repository orientation, README/specification/state/history scan, stored-memory lookup, Git status inventory, external search, previous program/result read, child delegation, canonical edit, commit or push. The inherited non-allowlisted worktree content was ignored. The mathematical read ranges are listed in SOURCE_PREFLIGHT.md. Heading-only scans were restricted to the five prescribed earlier memorandum files.

The ambient system/developer context included generic tool instructions and a supplied memory summary; no memory file was opened and that summary was not used as mathematical evidence. The parent supplied only the bounded assignment and a later status request. The branch-creation command emitted the base commit's one-line subject; this unavoidable metadata was not used to infer mathematical or audit status. The task's explicit isolation controlled over broader orientation and ledger instructions.

The allowed complete R18 manuscript included its own prose account of prior finite-dimensional diagnostics and source comparisons. This was treated as text within the expressly allowed earlier source. No associated old code, old result, R18 audit, R19 or R21 material, R20 constructor/proof/checker, current audit, root scratch or non-allowlisted mathematical file was read.

The only parent messages sent during work reported isolation, the proof route, analytic completion and remaining packaging work. No constructor narrative or comparison result was received.

## Writes and issuance

Writes were limited to the exact prescribed input overlay and control manifest; the assigned report AUDITS/BLIND_RECONSTRUCTION/ROUND_020_CONTINUOUS_PATH_RECONSTRUCTION.md; the unique ROUND_020_CONTINUOUS_PATH_BLIND_ARTIFACTS directory; and its sibling ZIP/seal. No output path existed before this work. A first draft-patch attempt was rejected for string escaping before creating the report; subsequent draft writes occurred before issuance. No issued artifact was amended or superseded.

The code in diagnostics/continuous_path_diagnostic.py was authored in this context. It uses only Python's standard library, exact rational arithmetic and formal finite exponential sums. There were no package installs, random samples, floating tolerances, copied checker code or external sources. It generated the result once, refuses to overwrite results, and supports read-only recomputation.

The final packet contains a byte-identical report copy, the exact fourteen inputs, the original control manifest, new source/exposure/README records, the new diagnostic/result and the new verifier. Every packet payload file is listed by SHA-256 in PACKET_SHA256SUMS.txt; its own hash and the archive/report hashes are covered by the sibling seal. Safe archive verification checks the complete member set and every byte. Files are marked read-only and directories nonwritable at issuance. This is a local immutable-by-convention issued packet, not a signature or tamper-proof external timestamp.

The worker stops at the sealed bounded handoff. Root alone may compare, arrange hostile/source gates, integrate state or decide promotion. Subsequent corrections must be separate superseding artifacts.
