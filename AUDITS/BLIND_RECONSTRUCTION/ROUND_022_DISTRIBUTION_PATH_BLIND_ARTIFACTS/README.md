# AUD063 — sealed whole distribution-path blind reconstruction

TASK097, issued 2026-09-18 UTC. This is the isolated whole-claim blind reconstruction of the unchanged THM044/PO031, from the sixteen prescribed source inputs. The report reconstructs the full stated uniform Hilbert-path theorem and both quantitative interfaces. Its disposition is PASS conditional on the unchanged complete-source premises. Current source-gate matching, comparison with the root construction, separate hostile review and canonical promotion remain root responsibilities.

The analytic proof supplies the source's simultaneous Fourier envelope, actual continuous Hilbert paths, a continuous decomposition, the uniform source first moment, the stated linear Fourier-tail second moment, Gaussian series existence and exact covariance, actual compact sets of paths and weak convergence against all bounded continuous functionals. It also supplies a stronger uniform Fourier-tail first moment for the actual paths. No first unproved analytic line remains in this conditional implication. This does not certify the broader research mission.

Origin:

- Worktree: `/Users/matthewrosenzweig/.codex/worktrees/hocf-r022-distribution-path-blind`
- Branch: `codex/hocf-r022-distribution-path-blind`
- Published base: `64ac0538dff37a37d0883661b401ad04c311a5e5`
- External report: `../ROUND_022_DISTRIBUTION_PATH_RECONSTRUCTION.md`
- Packet report copy: `RECONSTRUCTION.md`, required byte-identical to that external report.

Contents:

- `INPUTS/` — all sixteen complete, byte-verified authorized inputs, with their relative paths retained.
- `INPUT_SHA256SUMS.txt` — unchanged prescribed sixteen-input manifest.
- `RECONSTRUCTION.md` — complete analytic reconstruction, exact conjunction/negation and whole-claim dispositions.
- `SOURCE_EXPOSURE.md` — complete inspected-source inventory, hypotheses, inherited exposure and excluded sources.
- `round022_blind_diagnostic.py` — new exact diagnostic, written without reading a prior checker.
- `DIAGNOSTIC_RESULTS.json`, `DIAGNOSTIC_STDOUT.txt` — actual final results and concrete mutation witnesses.
- `verify_packet.py` — byte/member verifier, with hostile in-memory controls and no extraction.
- `OUTPUT_SHA256SUMS.txt` — every authored/control output except the two digest manifests themselves.
- `CONTENT_SHA256SUMS.txt` — every packet file except itself, including all inputs and the output manifest.

Actual diagnostic result: **PASS: 59,033 exact assertions in 27 categories; 15 nonvacuous mutations rejected.** Python 3.9.6, standard library only, exact rational and exponential-polynomial arithmetic. There is no random seed, sampling or floating-point tolerance. Controls challenge endpoint inclusion, loss of the dimension term, high-mode escape, compact Hilbert balls, inferred source second moments, reversed supremum logic, convolution sign/history, Brownian label factors, real Fourier normalization, transient covariance and noise normalization.

These finite computations support coefficient and inference checks. They do not certify infinite sums, singular passages, path existence, tightness or weak convergence. The complete report supplies those analytic arguments.

Executed diagnostic, from the origin worktree:

    python3 AUDITS/BLIND_RECONSTRUCTION/ROUND_022_DISTRIBUTION_PATH_BLIND_ARTIFACTS/round022_blind_diagnostic.py

The final execution was captured directly in DIAGNOSTIC_STDOUT.txt. To reproduce a diagnostic after issuance, work on a separately named writable copy, since the program writes its adjacent result file and the issued packet is read-only.

Sibling delivery files:

- `ROUND_022_DISTRIBUTION_PATH_BLIND_ARTIFACTS.tar.gz`
- `ROUND_022_DISTRIBUTION_PATH_BLIND_ARTIFACTS_ARCHIVE_MEMBER_SHA256SUMS.txt`
- `ROUND_022_DISTRIBUTION_PATH_BLIND_ARTIFACTS_SEAL.json`
- `ROUND_022_DISTRIBUTION_PATH_BLIND_ARTIFACTS_VERIFICATION.json`
- `ROUND_022_DISTRIBUTION_PATH_BLIND_ARTIFACTS_DELIVERY_SHA256SUMS.txt`

The archive has exact membership and only safe, unique, relative regular-file paths, with fixed read-only member modes; it has no links or extraction requirement. The archive-member manifest covers every archived file, including the packet content manifest. The external seal hashes the report, archive and principal manifests. The final delivery manifest hashes every delivered file other than itself, including that seal and the verification result; its digest is the final handoff anchor. This layered arrangement avoids recursive self-hashes while covering every byte.

Executed safe verification, from the origin worktree:

    python3 AUDITS/BLIND_RECONSTRUCTION/ROUND_022_DISTRIBUTION_PATH_BLIND_ARTIFACTS/verify_packet.py --require-readonly --write-result AUDITS/BLIND_RECONSTRUCTION/ROUND_022_DISTRIBUTION_PATH_BLIND_ARTIFACTS_VERIFICATION.json

For later read-only re-verification, omit --write-result so an issued result is never replaced. The verifier checks input and output coverage, every file hash, report-copy equality, diagnostic/program consistency, archive membership and bytes against disk, seal digests and read-only modes. Its seven hostile in-memory archive controls reject duplicate, missing, extra, changed-byte, symbolic-link, traversal and absolute-path members; additional direct path checks reject normalization tricks. None is extracted.

No canonical state, current history or root construction was read or edited. No old checker, outside source, private input, memory file, child agent, installation, commit, push, publication or contact was used. General inherited instructions and historical descriptions inside the authorized source proofs are disclosed in SOURCE_EXPOSURE.md. No TeX was edited or compiled; the requested proof is Markdown and the final handoff contains no mathematical LaTeX.

The external report, packet files, archive, manifests, seal and verification output are sealed read-only. Any correction requires a new separately issued artifact. Root alone performs source-gate matching, construction comparison, separate hostile review and any canonical integration.
