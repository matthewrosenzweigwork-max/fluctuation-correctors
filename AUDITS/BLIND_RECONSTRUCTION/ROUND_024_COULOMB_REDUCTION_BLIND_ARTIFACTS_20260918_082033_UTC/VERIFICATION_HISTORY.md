# AUD067 verification history

All entries here precede issuance. This record is frozen with the packet; the final read-only verifier's exact output is a named sibling JSON file covered by the outer seal.

1. The task card and 21-input SHA-256 allowlist were read, and the initial `shasum -a 256 -c` run passed every input before mathematical reading. No initial hash failed.
2. The 21 inputs were copied byte-for-byte into this packet. Each original was required to be a regular non-symlink file and its digest was reverified; each copy's content is covered by the same allowlist and final verifier. The manifest copy and 21-entry byte-count inventory were written.
3. Sources were read with full/partial/no-text distinctions recorded in SOURCE_EXPOSURE.md. One combined source read was display-truncated; the substantive full sources were reread as recorded. No truncated middle was silently counted as read.
4. A new exact-fraction Laurent checker was written. Its first completed run passed 594 assertions, including all 11 nonzero mutation witnesses; there was no failed assertion or mathematical test run.
5. Before issuance an uninformative one-line arithmetic check about a finite prefix was removed. The final checker run passed 593 assertions in 19 categories. All mathematical identity tests, solvable-model tests and 11 nonzero mutation witnesses remained. RESULTS.json is exactly its final deterministic JSON output.
6. The complete report was written and reread for all A/B/C clauses, the original target/negation, absolute versus signed norms, source qualifications, rate exponents, fixed-N domains, and every limiting/degenerate case. The source-prefix interpretation of asymptotic constants is explicit. No new claim resolving THM046 was introduced.
7. Before archive creation, every packet text is checked for final newline, forbidden control characters and trailing whitespace; the report's display delimiters are balanced. Inputs are checked again against their frozen original allowlist. No TeX source or build was involved.
8. The output digest manifest covers all report/artifact files except that manifest's own bytes. The archive membership/digest manifest additionally covers the output manifest. The archive contains exactly those member names, each uniquely and as a regular read-only Unix file. The report and artifact files are made read-only before the final verifier runs.
9. The portable verifier checks original copied-input digests, exact output bytes, the exact scoped file set, safe path components, regular read-only files, exact archive members/digests, archive CRC, and a fresh read-only rerun of all 593 assertions. Its exact JSON result is the named sibling VERIFICATION.json, which is then hashed in the outer seal. The verifier and checker perform no writes and use Python's no-bytecode option.
10. The outer seal covers the archive, archive member manifest, output manifest, and final verification JSON. The final `shasum -a 256 -c` of that seal is the issuance check. After it passes, report, packet, archive and seals remain byte-identical; any correction requires a distinct superseding artifact.

No assertion count is mathematical certification. The complete analytic argument is in the report; this is an isolated reconstruction and its own supporting verification, not an independent hostile review of that new report.
