# Audits

Store immutable reports under:

- `HOSTILE/`;
- `BLIND_RECONSTRUCTION/`;
- `SOURCE/`;
- `COMPUTATION/`;
- `EXTERNAL/`.

Never edit an issued verdict. Supersede it with a new version linked to the prior report.
