# AUD058 — whole-claim hostile review of THM041

2026-09-18 UTC. TASK089, fresh assigned Astra Max hostile lane. Worktree: `/Users/matthewrosenzweig/.codex/worktrees/hocf-r019-all-diffusivity-hostile`; branch: `codex/hocf-r019-all-diffusivity-hostile`; base: `e75f8b780682a7e9fb0715b5e8b5873be684a2e8`.

**Verdict: PASS for the complete frozen mathematical conjunction in THM041, on the exact fifteen-input dossier.** I found no admitted counterexample, invalid load-bearing estimate, unproved large-noise uniformity, or missing probability passage in the supplied root construction. Part A includes every finite nonnegative diffusivity throughout the stated sub-Coulomb/Coulomb range. Part B includes both suprema in the bounded-Lipschitz assertion, with its additional strict exponent restriction and all stated sequence consequences. No statement was repaired, weakened, edited, or substituted during this review.

This is a fresh **source-exposed hostile review**, not a blind construction, an independent certificate of the root's chronology, or canonical promotion. Earlier source histories remain exactly as issued. Root must still match the exact earlier source gates, compare the separate whole-claim reconstruction, and integrate. I did not inspect those gates, the reconstruction, canonical state, or history. Those administrative comparisons are outside this handoff; they are not an omitted analytic premise disguised by a theorem-card label.

## 1. Isolation, exposure, and exact target

The task card and hostile input manifest were the first files read. The assigned worktree was created at the specified published base, then exactly fifteen allowed files were overlaid and verified against all fifteen supplied SHA-256 digests. The same fifteen byte strings are preserved in the artifact packet. The general instructions to read broad orientation/state and to maintain canonical ledgers are superseded here by TASK089's narrower isolation and root-only integration contract.

No current/former blind proof, Round018 proof or audit, other audit record, canonical ledger, historical log, root scratch, external source, or referenced constructor checker/result file was opened. The permitted source memoranda themselves contain historical status statements and embedded descriptions of diagnostics; the entire allowed root memorandum, including its diagnostic summary, and the required root exposure/preflight were visible. This is recorded exposure, not evidence supplied by those diagnostics. The new program was authored from scratch and no other program was reused. The ambient instructions supplied a high-level memory summary; no memory file was opened and no memory-derived mathematical fact was used. No children, dependencies, commits, or pushes were used. Writes were limited to the exact source overlay, this report, and its unique artifact directory.

Root's required exposure file explicitly reports earlier campaign exposure, Round018 exposure, and unsolicited TASK088 summaries before the root seal. Its claims about what was written or opened when are coordinator attestations. I do not certify or reinterpret that chronology. None of the prior audit labels or progress summaries is used below as a mathematical premise.

The audited conjunction is precisely:

- **A:** For fixed integer `d>=3`, `0<s<=d-2`, fixed finite `T` and smooth real `h`, the genuine ordered/deleted statistic of the backward source satisfies `E_nu |P_N[J_(Q_(T-r)^nu h)]| <= C N^(s/d-1)` for all `N>=2`, all `r in [0,T]`, and every finite `nu>=0`, with one fixed-data constant. All original Haar contractions and actual singular dynamics are retained.
- **B:** Additionally `s<d/2`. For every fixed finite deterministic smooth time/test tuple, the supremum over finite `nu>=0` of its bounded-Lipschitz distance from the stated centered Gaussian tends to zero as `N` tends to infinity. Degeneracy, zero/repeated times, constant tests, arbitrary finite diffusivity sequences, finite-noise continuity, and hot-noise convergence to zero in probability are included.

The negation is an admitted fixed datum with failure of source/contraction integrability, exact Haar centering, the common Part A constant, or Part B and its sequence consequences. Failure of the uniform Gaussian assertion means that some admitted fixed tuple has an epsilon-positive distance along `N_l -> infinity` and finite `nu_l`; fixed-noise convergence is insufficient. Failure of Part A's constant is unboundedness of `N^(1-s/d) E|P_N|` over its actual `N,nu,r` quantifiers. A failed attempted bound, an inadmissible law, a changing test tuple, or a nondecaying bound outside Part B's range is not this negation.

## 2. Source and hypothesis preflight

The following are the portions used mathematically. References are to the frozen copies, not live replacements. Every file in the fifteen-input manifest is byte-locked; an included card gives scope, not proof.

| Source | Exact use and retained boundary |
|---|---|
| `AGENTS.md`; TASK089 | Conduct, whole-claim requirement, narrower isolation, allowed outputs, and root-only promotion. |
| `ROUND_001_MODEL.md`, lines 11–44 | Unit Haar torus; characters `exp(2 pi i k.x)`; coefficient-one kernel; interaction `1/N`; independent noise `sqrt(2nu)`; ordered denominator `N^2`; `P=U2/2`; centering and scaling. |
| `ROUND_001_ALGEBRA.md`, Sections 1–2 and 5 | Smooth one-body symmetrization and bracket coefficients; used as comparison, then rederived on actual stopped singular paths. No smooth singular-diagonal assignment is transferred. Unused pair/hierarchy results are not premises. |
| `ROUND_004_SINGULAR_RESPONSE.md`, Sections 2–3, relevant Section 5 heat facts | Exact heat/Fourier normalization, local coefficient one, `K in L1`, full finite divergence measure, its lower bound, and heat approximation. The atom and compensation are retained. No singular pair propagator is used. |
| THM026; `ROUND_006_SINGULAR_PARTICLE_REALIZATION.md`, Sections 1–8 | The card retains its issued OPEN history. The complete energy-localization, measurable realization, noncollision, and same-noise heat proofs establish the actual finite-parameter paths needed here. Their local constants can depend on `N,nu` and separation. No uniform-in-noise heat convergence rate is imported. |
| `ROUND_010_ACTUAL_LAW_FALSIFICATION.md`, Sections 2–3 | Smooth free-energy differentiation, shifted Fatou, pair moment, sharp-retention self subtraction, and actual empirical Fourier bound. The former bounded-noise statement is checked at the proof level. Other corrector/domain/entropy conclusions in that memorandum are not needed. |
| THM038; `ROUND_016_SOURCE_EXTENSION.md`, Sections 2–7 | The card's bounded upper diffusivity is not extrapolated. The entire positive-retention, logarithmic-symbol commutator, remainder domination, and contraction proof is inspected. Its constants require only the actual expected-energy sign and spatial seminorms. No pair corrector, fluctuation theorem, or generic-law coercivity is assumed. |
| `ROUND_017_CRITICAL_FINITE_DIMENSIONAL_GAUSSIAN.md` | Read in full; its issued conditional critical theorem is not used as a positive-noise theorem. Sections 1–2 supply a comparison for the direct stopped identity. Its Gaussian-smoothing exposition is not a substitute for checking the new two suprema. |
| THM041; `ROUND_019_UNIFORM_DIFFUSIVITY_GAUSSIAN.md` | Complete frozen target/negation and all Sections 1–9 of the root construction. The root assertion of no unresolved line and its diagnostic counts are not proof. |
| Required root `EXPOSURE.md` and `SOURCE_PREFLIGHT.md` | Exposure and dependency claims are checked against the sources; these files do not certify their own chronology or mathematics. |

The underlying law is always actual homogeneous gradient dynamics from iid Haar preparation independent of the Brownian drivers. Positive-time product law is never required. The geometry remains the unit torus, the force is repulsive `K=-grad g`, the external drift is zero, and only finitely many fixed smooth tests and finite times are admitted. Every diffusivity in a supremum is finite. No private source or unverified bibliographic theorem is indispensable; there was no external literature or novelty assessment.

## 3. The all-diffusivity source estimate survives

### 3.1 Normalization and actual trajectories

Write `alpha=(d-s)/2>=1` and `A=4^alpha pi^(d/2)/Gamma(s/2)`. The periodized Gaussian heat integral `A integral_0^infinity u^(alpha-1)(p_u-1) du` has nonzero coefficient

    A Gamma(alpha)/(4 pi^2 |k|^2)^alpha
      = pi^(s-d/2) Gamma(alpha)/Gamma(s/2) |k|^(s-d).

Its central Euclidean Gaussian integral is exactly `|x|^(-s)`. The remaining periodized contribution is smooth locally at the puncture. Thus `K` has order `|x|^(-s-1)` and is Haar integrable because `s+1<d`, including the endpoint. The punctured integration by parts has inner flux `s r^(d-s-2) integral_S a(r theta)`. This tends to zero below Coulomb and to `(d-2)|S^(d-1)| a(0)` at Coulomb. Fourier coefficient zero fixes the compensation. Consequently

    D=div K=s(d-2-s) g_(s+2) dx                 below Coulomb,
    D=c_d (delta_0-dx), c_d=(d-2)|S^(d-1)|     at Coulomb.

Both are finite signed measures with total mass zero and `D>=-kappa dx`; their nonzero multipliers are the positive `D_k` in THM041. Dropping the Coulomb atom would give the wrong nonconstant response, while dropping compensation would destroy constants. There is no use of a logarithmic limit.

For the actual paths, `H_N=N^-1 sum_(i<j) g(X_i-X_j)` has gradient equal to minus the full drift. After subtracting `(N-1)g_*/2`, every summand is nonnegative and any partial collision sends the energy to infinity. Its configuration Laplacian is exactly `(2/N) sum_(i<j) Delta g <= (N-1)kappa`, with both coordinates differentiated. The stopped Itô identity has drift `-|B|^2+nu Delta H_N` and bracket `2nu integral |B|^2`. The exit probability is at most `[shifted initial energy+nu(N-1)kappa T]/R`. Letting `R` tend to infinity at each fixed finite tuple establishes noncollision and global uniqueness; the measurable cutoff construction permits iid initial randomization. No pairwise square is substituted for the total drift square.

Each such realized path has a positive minimum separation on a compact horizon. Coupled heat drifts converge locally in `C1`, their Brownian terms cancel, and collision-excluded Gronwall prevents the comparison stop for sufficiently small heat parameter. This proves the fixed-tuple heat limit used next. It does not assert a uniform-in-noise random separation or uniform heat rate, and neither is needed by THM041.

### 3.2 The relevant energy bound has no upper-noise constant

At every fixed positive finite `nu` and particle heat cutoff, the smooth configuration law has initial density one. Direct integration by parts gives

    d/dt [nu Ent(F_t)+E H_N^epsilon]
      =- integral F_t |grad H_N^epsilon+nu grad log F_t|^2 <=0.

Both initial terms vanish, and entropy is nonnegative on unit mass. Therefore `E H_N^epsilon<=0`; its right side contains no diffusivity-dependent factor. The common lower bound is `(N-1)g_*/2`. Pathwise local kernel convergence and shifted Fatou yield `E_nu H_N(t)<=0` for each finite `nu`, hence the same inequality for all of them. The shift depends on `N`, which is legitimate because this limit is performed at fixed `N` before any large-particle estimate. At `nu=0`, the actual deterministic gradient flow decreases energy and the same initial mean is zero. Dividing by noise is unnecessary.

Permutation equivariance gives exchangeability. Thus `E g(X_1-X_2)=2 E H_N/(N-1)<=0`, and `|g|<=g+2|g_*|` gives a common bound on `E|g(X_1-X_2)|` and the inverse-`s` pair-distance moment. This is the actual-law integrability needed for the genuine source. It is not a squared-force estimate or a product-law statement.

The sharp heat retention with positive coefficients has the exact lower bound

    H_N >= (N/2) sum_(k!=0) a_r(k)|eta_hat(k)|^2
             -g^>r(0)/2 -(N-1) A r^alpha/(2alpha).

The self factor is `1/2`, not `1/(2N)` for `H_N`. Taking expectation and `r=N^(-2/d)` bounds the Fourier energy by `C N^(-(1-s/d))`. For `|k|<=N^(1/d)`, the retained interval `[|k|^-2,2|k|^-2]` gives the lower symbol bound; above this frequency use `|eta_hat|<=1`. This proves exactly R19 (2.1), with constants independent of all finite noise and deterministic time. Weighted absolute Fourier summation then gives (2.2); it needs no estimate of a singular force square.

### 3.3 Positive retention and every original contraction

For the separate observable scale `r`, take `M=d+2`, `w_r(u)=(1-exp(-u/r))^M`, and `psi_r=1-w_r`. The exact split `g=g_r+Q_r-c_r` has `Q_r>=0`, `c_r=integral Q_r=C r^alpha`, `g_r` at least `C2`, positive retained coefficients, and `g_r(0)<=C r^(-s/2)` through scale two. The smooth self subtraction and all ordered deleted pairs give

    H_N/N=E_r+S_r-g_r(0)/(2N)-(N-1)c_r/(2N),
    E_r=(1/2)sum a_r(k)|eta_hat(k)|^2>=0,
    S_r=(1/(2N^2))sum_(i!=j) Q_r(X_i-X_j)>=0.

Hence `E(E_r+S_r)<=C[N^-1 r^(-s/2)+r^alpha]` uniformly over finite noise. There is no positivity claim for an arbitrary weighted Riesz kernel.

The logarithmic-symbol estimate follows by integrating the derivative of `u^alpha w_r(u) exp(-lambda u)` and using `0<=u w'_r<=M w_r`. Its radial bound is `0<=-xi a'_r/a_r<=L=2(alpha+M)`, uniformly in `r`. For nonzero lattice vectors, `min(|k|,|ell|)>=1`, `max/min<=1+|k-ell|`, and the radial monotonicity and log bound give the stated R19 lattice estimate. Symmetrizing the Fourier force produces `-pi i [k a_r(k)-ell a_r(ell)]`, with the zero signed-measure mode absent. Shifted-sequence Cauchy–Schwarz gives `|P_N[J_gr]|<=2 C_v E_r`. All factors agree with `P=U2/2`.

For the omitted source, termwise differentiated Gaussians give `dist(z)|grad p_u(z)|<=C_d p_(2u)(z)`. The torus geodesic bound on `v(x)-v(y)` and substitution `a=2u` give `|J_(g-gr)|<=C_d 2^(-alpha)||Dv||_infinity Q_(2r)`. The empirical absolute value contributes `S_(2r)`, the mixed Haar contraction `c_(2r)`, and the scalar contraction `c_(2r)/2`. Their integrability and bounds follow from the same positive split. The scalar is actually zero for the original gradient source, but its coefficient is not erased in the estimate.

The remainder gradient is Haar `L1` because `alpha>=1`; actual source growth is at most `C(1+dist^-s)`. Thus this really is the original observable and all its contractions, not a assigned-diagonal surrogate.

For `v=grad Q_(T-r)^nu h`, every spatial Fourier multiplier is at most one in absolute value, even at arbitrarily large finite noise. The seminorm controlling `C_v` requires a fixed power at most `alpha+M+3` of the Fourier weight of `h`, while `||Dv||_infinity` requires two spatial derivatives. These are finite for the unchanged smooth terminal test. A uniform terminal time derivative would be false for a nonconstant mode as `nu` diverges; the construction correctly requires it only for each fixed finite parameter in the local identity. The source estimate itself never uses that false uniformity. Choosing `r=N^(-2/d)` gives both errors exactly `N^(s/d-1)`. **Part A passes without a noise cutoff, including zero noise and all of its stated exponents.**

## 4. Actual vector identity, centering, and dependence

Common translation of the initial positions translates the entire path by uniqueness, with the same Brownian increments. Its law stays invariant under every fixed torus translation. This proves every one-body marginal is Haar by its Fourier coefficients; no single exceptional set uniform over all translations is needed. This checks exact centering rather than a small bias estimate.

For each fixed finite `nu`, the backward test satisfies `partial_r f+nu Delta f-D*f=0`. Directly integrating the off-diagonal source gives the row `j_f=-D*f`, and the scalar row integral is zero. At Coulomb the row is `-c_d(f-integral f)`. The ordered force sum is precisely half the deleted source sum, so its decomposition is `P_N[J_f]+eta_N[j_f]`. This cancels the backward linear term with the stated sign.

Time-dependent Itô is valid on collision-excluded stops. Its endpoint is smooth, the symmetrized drift is integrable in probability times time by the pair moment, the row is bounded, and bounded gradients pass martingales in `L2`. Stops exhaust each compact horizon by actual noncollision. Thus the unstopped identity is genuine for every finite tuple and gives R19 (4.1), with

    A_j=sqrt(b/N) sum_i (Q_tj^nu h_j(X_i(0))-integral h_j),
    M_j=sqrt(2nu b/N) sum_i integral_0^tj grad f_j,r(X_i(r)).dW_i(r),
    R_j=sqrt(Nb) integral_0^tj P_N[J_fj,r](X(r)) dr.

The independent Brownian labels give no mixed-label trace. `nu b<=1` gives true square-integrable coordinate martingales. Part A and `b<=1` yield `sup_nu E|R|<=C N^(-kappa)` in Euclidean norm, where `kappa=1/2-s/d>0`. This uses only an absolute first moment.

For any real coefficient vector `u`, the combined stopped/extended martingale has deterministic spatial integrand `G_r=sum_j u_j 1_(r<=tj) grad f_j,r`. Deterministic endpoint indicators are predictable; changing their finitely many endpoint values has no effect. Its exact bracket and Haar comparison are

    q=2nu b integral eta_N[|G_r|^2] dr,
    v=2nu b integral integral |G_r|^2 dx dr.

Weighted Fourier convolution bounds for the product `|G|^2`, R19 (2.2), and `nu b<=1` imply `E|q-v|<=C |u|^2 N^(-(1-s/d)/2)`. Independently, bounded gradients give the pathwise bound `q<=C|u|^2` and the analogous deterministic bound for `v`. Neither product law nor singular squared-source moments appear.

Finite-particle dependence between `A` and `M` is genuine. The process `exp(i M_r+<M>_r/2)` has differential `i exp(i M_r+<M>_r/2)dM_r`. Its bounded modulus and bracket make it a true complex martingale, with conditional expectation one given the initial sigma-field. Because `A` is initial-measurable,

    exp(-v/2) E exp(i A)
      =E[exp(i(A+M)) exp((q-v)/2)].

The mean-value bound for the real exponential uses only the common bracket bound, producing R19 (5.3) with constants bounded on compact `u` sets. This is an asymptotic factorization argument, not finite-particle independence or substitution of the random bracket by its mean. The compensator sign is correct.

## 5. Gaussian covariance and both uniform suprema

The initial summands `V_i^nu` are iid, centered, and uniformly bounded in norm across all finite noise: their fixed spatial Fourier sum and `b<=1` suffice. Taylor's cubic remainder is bounded by `C |u|^3 N^-3/2`; the logarithm expansion is uniform on each compact `u` set for all sufficiently large `N`, independently of `nu`. It gives the stated initial triangular characteristic approximation with covariance `B(nu)`. Adding the source and dependence errors gives compact-uniform characteristic convergence to `exp(-u^T C(nu)u/2)` with error at most a compact-set constant times

    N^(-(1/2-s/d)) + N^(-(1-s/d)/2) + N^-1/2.

Both exponents from the actual law are positive exactly where used. Covariance is the sum of the initial and deterministic thermal Gram matrices, so is real positive semidefinite even if singular. A centered Gaussian therefore exists as `C(nu)^(1/2) xi`, without any inverse matrix or density assumption.

For one paired nonzero mode, the initial contribution is `b exp(-(ti+tj)L)`. The Brownian contribution has the exact factor

    2nu b a integral_0^min(ti,tj) exp(-(ti+tj-2r)L) dr
      =b (nu a/L) [exp(-|ti-tj|L)-exp(-(ti+tj)L)].

Their sum is exactly THM041's formula, because `L=D+nu a`. Smooth Fourier coefficients justify summation and integration; real conjugate pairing gives the stated real covariance. Constants vanish through centering before the zero mode is excluded. At zero noise only the initial sum-of-times term remains. At zero or repeated times the same formula applies. Each diagonal is bounded by `b ||h_j-integral h_j||_2^2` since its two nonnegative coefficient fractions sum to one.

The explicit tightness and smoothing passage is valid with the source's available integrability. Set `W=A+M`. Its second moment is uniformly bounded by twice the two separate second moments; independence is not needed. Splitting `|Z|>R` between `W` and the residual gives

    sup_nu P(|Z|>R) <= C/R^2 + C N^-kappa/R.

This is uniform also over `N>=2`; no second moment of `R` was asserted. The Gaussian comparison has a uniform second moment, and adding independent Gaussian smoothing retains uniform tails.

At fixed positive smoothing variance, the Gaussian Fourier representation is justified by Fubini with an integrable multiplier. Split frequency space into a fixed ball and its complement. Compact-uniform characteristic convergence controls the first piece uniformly in `nu`; the bound two and the integrable Gaussian tail control the second. Thus smoothed density difference tends to zero uniformly in spatial point and diffusivity. This uses a bound for every `nu`, not measurability of an uncountable supremum.

For all real tests with sup norm at most one, split the expectation over a fixed spatial ball and its complement. Ball volume times the density sup error bounds the inside, uniformly over both the test and `nu`. The actual and comparison tails bound the outside. Let `N` tend to infinity and then the spatial ball expand. This proves convergence uniformly over those bounded tests for the smoothed laws. For Lipschitz constant at most one, coupling the smoothing changes either expectation by at most `sqrt(epsilon) E|xi|`, uniformly in all parameters and tests. Finally let epsilon decrease to zero. **This proves both the test supremum and the all-finite-diffusivity supremum demanded by Part B.** Pointwise convergence or covariance agreement alone would not suffice; the proof does not stop there.

## 6. Sequence consequences and edge cases

For finite `nu_bar`, `b(nu)` is continuous, including zero and the corner at one. Each covariance summand is continuous and bounded by a fixed summable smooth Fourier product. Hence the covariance matrices converge. Continuity of positive-semidefinite square roots follows from uniform polynomial approximation of the square-root function on a common bounded spectral interval. Coupling with a common standard Gaussian proves convergence in expected norm, so the uniform bounded-Lipschitz approximation gives the asserted finite-noise weak limit.

If `nu_N -> infinity`, the diagonal bound makes `trace C(nu_N)<=b(nu_N) sum_j ||h_j-integral h_j||_2^2 ->0`. The Gaussian law tends to zero in bounded-Lipschitz distance, and the uniform approximation transfers this to the actual vector without a relation between its diffusivity growth and `N`. To spell out the threshold conversion in R19 line 186, with `c=max(1,1/epsilon)`, the admitted test is `F(x)=min(1,|x|/epsilon)/c`; then

    P(|Z|>=epsilon) <= c E F(Z).

The fixed factor `c` is harmless. The sentence that the test bounds the probability must be read with this displayed normalization factor; this is an explicit check of the argument, not a change to its assertion or a new hypothesis. Infinity is never a particle SDE coefficient.

Constant tests, `T=0`, repeated and zero observation times, linearly dependent tuples, and degenerate covariance need no exceptions. The endpoint `s=d/2` does not give a vanishing source residual and remains excluded from Part B. In particular three-dimensional Coulomb is included in both parts, whereas four-dimensional Coulomb is Part A only. No claim about path-space or distribution-valued tightness, growing tuples, general preparation, inhomogeneity, logarithmic kernels, exponents above Coulomb, or hierarchy closure has entered. The old energy-floor, full microscopic-subcritical, and positive finite critical conditions remain distinct; broad applicability within this restricted row does not identify them.

## 7. Per-claim dispositions

All dispositions concern the exact supplied bytes. Confidence is high for these analytic conclusions within the frozen scope; diagnostic probes are supporting exact checks, not stand-alone proof certificates.

| Stable audit item | Root location | Disposition and decisive check |
|---|---|---|
| AUD058-C01 | THM041 lines 5–30; R19 Sections 1, 8 | PASS — exact conjunction/negation, quantifiers, law, geometry, normalization, and exclusions preserved. |
| AUD058-C02 | R19 lines 11, 15–22 | PASS — actual fixed-finite-parameter construction and heat passage; expected-energy sign independent of upper noise, including zero. |
| AUD058-C03 | R19 lines 24–44 | PASS — exact smooth self subtraction, all-mode Fourier bound, and smooth-test moment bound with common constants. |
| AUD058-C04 | R19 lines 48–68 | PASS — positive retention, deleted background coefficient, logarithmic symbol estimate, and commutator factor. |
| AUD058-C05 | R19 lines 70–81 | PASS — omitted positive kernel at twice the scale, all contractions, actual integrability, spatial/time distinction, full Part A. |
| AUD058-C06 | R19 lines 85–103 | PASS — exact Haar centering, stopped singular identity, genuine row and Coulomb atom, residual norm and strict exponent. |
| AUD058-C07 | R19 lines 107–123 | PASS — true combined martingale, independent-label coefficient, bracket fluctuation and pathwise bracket bounds. |
| AUD058-C08 | R19 lines 125–135 | PASS — initial-sigma-field conditional exponential identity, correct sign, no independence assumption. |
| AUD058-C09 | R19 lines 139–153 | PASS — initial triangular iid expansion uniformly in noise and compact coefficient sets; all three characteristic errors. |
| AUD058-C10 | R19 lines 155–164 | PASS — full cross-time covariance, Gram positivity, exact thermal factor, zero modes/times and diagonal cap. |
| AUD058-C11 | R19 lines 168–180 | PASS — genuine tight tails from only residual `L1`, degenerate Gaussian construction, Gaussian smoothing, both metric suprema. |
| AUD058-C12 | R19 lines 184–186 | PASS — finite-noise continuity and unrestricted hot-noise convergence in probability; threshold normalization checked explicitly. |
| AUD058-C13 | R19 lines 188–190 | PASS AS LIMITED — no new scope inferred; no analytic first-unproved line found in this hostile review. No canonical promotion is assigned here. |
| AUD058-C14 | Root exposure/preflight; R19 lines 3, 195–197 | EXPOSURE RECORDED — root chronology is attested, not independently certified; reported earlier diagnostics and accepted gates were not evidence for this verdict. |

There is no finding that requires mathematical repair. The strongest surviving statement is the entire frozen THM041 conjunction. The remaining actions are exact source-gate matching, comparison with the separate reconstruction, and root integration, not extra assumptions or a narrowed theorem silently substituted by this reviewer.

## 8. Fresh adversarial diagnostics and their limits

`fresh_exact_diagnostic.py` is newly authored, standard-library Python. It uses integer/rational arithmetic, exact Gaussian rational Fourier values, and formal polynomial identities. No random seed, floating-point tolerance, simulation, outside program, or numerical inference is used. Final run: **7,564 assertions across 40 categories; 879 rejected mutations in 24 families.** The full counts and first exact witness in each mutation family are in `DIAGNOSTIC_RESULTS.json`.

The diagnostic is substantially broader than covariance:

1. Literal `N=2,3,4` source/force sums at distinct torus points independently recover ordered deletion, both self/background energy subtractions, the row, the zero scalar, the symmetrization half, and the Fourier commutator. Spatial derivatives are represented after dividing out `2pi`; the physical source and generator terms have the common `(2pi)^2` factor, restored analytically above. These are regularized/smooth algebra tests, not a claim that a finite Fourier kernel is the singular Riesz kernel.
2. Exact finite-sum Laplace integrals of the polynomial retention at integer `alpha=1,2,3` verify positive coefficients and omitted masses, radial-log bounds, and 3,000 squared lattice inequalities over scales from `1/1000` to `1000`. An explicit sharp exponential-retention witness rejects reusing its neighbor-weight ratios as if they had the polynomial bound. The analytic argument, not this finite grid, covers arbitrary admitted real exponents and all scales.
3. The free-energy square is independently checked with every entropy/noise factor over diffusivities through `10^20`. A nonconstant terminal mode rejects a hypothetical uniform-in-noise time derivative while keeping its spatial amplitude fixed.
4. On the **fixed physical time grid `j/3`**, covariance is checked as a formal polynomial in `q=exp(-(D+nu a)/3)`. A sum of initial and independent increment Gram contributions telescopes to the frozen formula for arbitrary `q`, so no parameter-dependent physical times are substituted. Diffusivities include zero, the cap corner, and hot values. Literal binomial iid moment sums independently check the initial triangular fourth/sixth corrections, and repeated-time rank-one and constant-test degeneracies are retained.
5. For initial `S=+1/-1` and an independent Brownian motion, set `exp(iA)=S`, `q_S=2 log 2+2S log((n+1)/n)`, and `M=sqrt(q_S)B_1`. All relevant characteristic exponentials and compensators are exact rationals. The correct random compensator has the conditional identity; finite-N independence, the wrong sign, and replacing the bracket by its mean fail. The actual factorization error nevertheless tends to zero at the predicted bracket-concentration scale. This directly challenges dependence, not only covariance.
6. A residual equal to `+/-n^4` with total probability `n^-5` has absolute first moment `1/n` and second moment `n^3`. It validates the first-moment tail mechanism and rejects an unjustified source `L2` promotion. Moving parameter spikes reject a pointwise-to-uniform-noise inference. Widely separated expanding uniform densities reject use of a density sup bound without tight tails. The normalized bounded-Lipschitz threshold test checks the exact factor needed to conclude probability convergence.

The adapted-mixture, heavy-residual, moving-spike, and expanding-density examples are deliberately independent falsification routes for proof steps. They are **not** counterexamples in the admitted singular-particle law class. Their role is to distinguish the actual proof's valid arguments from tempting invalid substitutes. Exact arithmetic and rejected mutations do not certify the infinite-dimensional source estimates or a singular limiting theorem on their own.

## 9. Reproducible, sealed bounded handoff

Created outputs are this report and `AUDITS/HOSTILE/ROUND_019_ALL_DIFFUSIVITY_HOSTILE_ARTIFACTS/`, containing the full report copy, README, source/exposure preflight, fifteen exact input copies, the fresh program and actual JSON result, verification/sealing code and manifests, complete verified archive, and seal verification record. Input/source edits were only the prescribed byte-identical overlay in the isolated worktree. No canonical source, ledger, earlier issued report, branch history, or remote was altered.

The executed mathematical command was:

    python3 AUDITS/HOSTILE/ROUND_019_ALL_DIFFUSIVITY_HOSTILE_ARTIFACTS/fresh_exact_diagnostic.py

The verifier reruns that command in memory, requires byte-identical results, validates every allowed input and output byte, checks an exact complete archive-member set with no traversal/absolute paths/duplicates/symlinks/special entries, compares all archived bytes without extracting, checks the archive digest, and verifies read-only modes. `OUTPUT_SHA256SUMS.txt` and the external archive seal avoid recursive self-hashing. The final `SEAL_VERIFICATION.json` records exact counts and hashes. All the files reported in this handoff exist in their intended final locations.

No TeX or PDF was generated or modified: this issued audit is Markdown and the final handoff contains no mathematical LaTeX. There was no failed mathematical test in the final run and no new unverified reference. The complete audit and byte verification are distinct evidence: neither a program PASS nor an archive digest substitutes for the analytic review.

Stop at this sealed bounded handoff. Root alone decides promotion after matching the two independent axes and exact prior source gates. If a later issue is found, issue a new superseding report; do not edit these issued bytes.
