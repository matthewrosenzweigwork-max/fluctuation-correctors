# Whole candidate claim for fresh review

This is an unnumbered worker claim. Root owns campaign identifiers and
independent promotion. Status: SELF-CHECKED only. See REPORT.md for every
definition, coefficient, proof, domain passage and exact target/negation.

Fix the exact actual iid-Haar four-dimensional coefficient-one Coulomb
particles of THM046, its real smooth h, finite T, unchanged deleted source
and backward response. Fix 0<lambda_lower<=lambda_upper<infinity.
For all integer N>=max(2,ceil(lambda_lower^-2)) and all beta>0 with
lambda_lower<=beta/sqrt(N)<=lambda_upper, put nu=1/beta and
S_N=sqrt(N) times the time-integrated literal source. On this index range
beta>=1, so sqrt(N) is exactly the frozen sigma_N.

The whole conjunction is:

1. There exist finite nonnegative A,C depending only on the fixed kernel,
   h,T,lambda_lower,lambda_upper, never N or beta, such that
   E[(abs(S_N)-A)_+^2]<=C(1+log N)/N.
2. Along every admitted critical sequence, the family abs(S_N)^2 is
   uniformly integrable, and E abs(S_N) tends to zero if and only if
   E abs(S_N)^2 tends to zero.
3. With the exact true one-body martingale retained, both are equivalent
   to E abs(sqrt(N)(rho_T[h]-rho_0[Q_T^nu h]))^2 tending to zero.
4. The full THM046 assertion for all its fixed smooth real tests is
   equivalent to REPORT.md (8.7) for all fixed nonzero Fourier modes and
   finite horizons. Their admitted positive-limsup negations are equivalent.

The exact negation is an admitted fixed datum/window for which any clause
fails. Failure of clause 1 means that for every finite A,C there is one
of the specified N,beta with excess larger than its asserted bound.
Failure of clauses 2--4 means an admitted sequence violating precisely
the stated integrability/limit/equivalence, with its original law and
scaling retained. A proof-route failure does not negate this conjunction.

The claim does NOT assert S_N tends to zero, that any remaining signed
correlation cancels, finite second moment of the instantaneous source or
unclipped initial energy, positive-time independence, Gaussian limits,
full corrector closure, or a new law class. The primary THM046 assertion
and exact negation remain unchanged and open.

Load-bearing checks: the pointwise heat-split majorant; exact energy
martingale and action bound; canonical clipped-energy covariance,
including overlapping labels; initial-event union bound; conditional
isometry restricted to a time-zero event; original endpoint source sign,
background coefficients and shared-driver cross bracket; the quantitative
L1/L2 transfer; all correlation overlap and response-slot factors.
