# AUD070 — whole THM048 and THM049 hostile review

TASK109. Issued 2026-09-18 UTC. Assigned fresh Astra Max hostile context, worktree `/Users/matthewrosenzweig/.codex/worktrees/hocf-r025-source-ui-hostile`. The task records branch `codex/hocf-r025-source-ui-hostile` and provisioned base `06bf56dae256085646228aa28159210b567bf409`; no Git history or metadata was read to reverify that administrative statement. This report is the hostile gate only. Root owns comparison with the withheld reconstruction, exact accepted-source matching, promotion and canonical state.

**Whole THM048: PASS. Whole THM049: PASS. Joint conjunction: PASS.** No load-bearing mathematical defect or admitted counterexample was found. Both complete frozen cards, not merely their strongest displayed estimates, were reconstructed. The verdict includes the finite initial segment, endpoint-defect L1 and L2 assertions, both uniform-integrability conclusions, the actual signed-correlation identity, the every-smooth-real-test equivalence and its exact positive-limsup negation. THM046 and the cancellation in candidate (8.7) remain OPEN. Neither is a premise or a conclusion of this audit.

**Ancillary mathematics: PASS within the stated scopes. Evidence: independently checked as specified below; withheld constructor diagnostics and historical certifications are not verified here. Typesetting/formula transcription: PASS on the complete Markdown source, with one administrative line-count discrepancy recorded separately.** No candidate, theorem card or other input was edited or silently repaired. There is no first failing mathematical line to report for either frozen conjunction.

## 1. Exact authority and scope

The supplied manifest `AUDITS/ROUND_025_SOURCE_UI_HOSTILE_INPUT_SHA256SUMS.txt` contains exactly 15 rows. Every SHA-256 matched before substantive mathematics and again when copying the inputs. The complete candidate has SHA-256 `20e3d6b15ae823eb8eb618e3578878c66d2835feca7fe868e5eab1060e1a84ea` and **687 lines**. Every line was displayed and reviewed. TASK109's description of a 663-line candidate is an administrative count error, not permission to truncate. Root subsequently confirmed that the verified hash is authoritative. The frozen bytes were retained.

THM048 is the conjunction of genuine finite-N L2, the stated critical-tail squared-overshoot estimate with its prescribed constant dependence, squared uniform integrability of source and endpoint defects including finite prefixes, and equivalence of all four vanishing criteria and their positive-limsup failures. Its negation requires an admitted violation of one of those clauses. Infinite instantaneous source variance, the failure of a proof attempt, or nonvanishing of the still-open source itself would not negate THM048.

THM049 is the conjunction of absolute integrability of the actual finite-N correlation integrals, the exact identity and explicit-term rates, necessity and sufficiency of the fixed-mode signed criterion for the original every-fixed-smooth-real-test source assertion, and the corresponding existence-of-a-fixed-mode positive-limsup criterion. Its negation is an admitted violation of one of these statements, not a witness that the unknown cancellation fails. THM048 was reconstructed in this same fresh hostile context before it was used in reviewing THM049.

The constructor's separate CLAIM_CARD has a stronger finite-window quantifier: one pair of constants for every N above `max(2,ceil(lambda_lower^-2))` and every beta in that window. This was checked separately as ancillary mathematics. It does not replace either root-frozen card or enlarge the declared theorem verdict.

The review uses only the coefficient-one periodic Coulomb kernel in dimension four, iid initial Haar positions independent of the Brownian drivers, positive finite diffusivity at each N, the literal ordered deleted statistic and its Haar contractions. No singular pair inverse, THM047, current audit result, product law at positive time, uniform N-body density estimate, logarithmic normalization or different preparation is a premise.

## 2. Whole-candidate coverage and source qualifications

| Candidate lines | Material reviewed | Disposition |
|---|---|---|
| 1–89 | Status, exact model, target/negation, regime separation and source qualifications | Consistent with the frozen cards; historical certification is not imported. |
| 91–167 | Kernel normalization, actual singular realization, true energy martingale, fixed-N density bound, exchangeability and Haar one-body marginals | Recomputed below; PASS. |
| 169–267 | Positive heat splitting, all self/background terms, Fourier commutator, absolute source majorant and pathwise energy bound | Recomputed pointwise, not just in expectation; PASS. |
| 269–313 | Heavy initial energy tail, canonical clipping, overlapping-label variance, initial bad-event probability and exact mean shift | Recomputed; PASS. |
| 315–382 | Literal endpoint identity, martingale and cross brackets, bounded endpoint and conditional isometry | Recomputed on actual stopped and unstopped paths; PASS. |
| 384–464 | Full finite-window assertion/negation, overshoot estimate, squared UI, L1/L2 and endpoint transfer, positive-limsup and degenerate cases | PASS, including the additional endpoint clauses explicitly frozen in THM048. |
| 466–514 | Infinite energy variance, radial formulas and diagnostic claims | Analytic formulas PASS. Constructor execution counts are read as claims, not rerun or adopted as evidence. |
| 516–631 | Current/current and current/initial counts, brackets, scalar equations, exact correlation identity, mode-to-smooth and fixed-real-witness passage, alternate pair formula | Recomputed; PASS. |
| 633–687 | Adversarial qualifications, exact open line, status and handoff statements | Consistent with a candidate reduction, not a proof or disproof of THM046. Historical packet-integrity assertions are not independently verified. |

The precise read/exposure record is supplied in `SOURCE_READ_EXPOSURE.md`. R6 and R16 were read completely after an initially truncated combined tool display; no undisplayed part was treated as read. R1 was read through line 250, R4 through line 190, and R10 through line 240. The remaining bytes of those three reports were hash-checked and copied but are not represented as mathematical reading. All needed inputs were independently reconstructed in the exact d4,s2 row. References from permitted files to nonallowlisted material were not followed.

## 3. Singular kernel, actual paths and the true energy martingale

The Fourier normalization determines

\[
g=c\int_0^\infty(p_u-1)\,du,\qquad c=4\pi^2.
\]

The nonzero coefficient is `c/(4*pi^2*|k|^2)=|k|^-2`. In four dimensions the central Gaussian integral is exactly `|z|^-2`: substituting `v=|z|^2/(4u)` gives coefficient `c/(4*pi^2)=1`. On a small coordinate ball the remaining lattice terms have exponentially small small-time bounds after every derivative; the subtracted Euclidean large-time terms are integrable. Thus the local smooth even remainder and the stated force are justified. The principal force flux is `2*|S^3|=4*pi^2`. The zero Fourier mode supplies the negative constant, so

\[
\operatorname{div}K=c(\delta_0-dz),\qquad
\Delta g=c\quad\hbox{off zero}.
\]

These are different identities with different uses. The punctured identity is appropriate for localized particle Itô calculus; the full distribution is required for the test row and the heat drift. The kernel and force are Haar L1, since their local powers are respectively `r^-2` and `r^-3` against four-dimensional volume. The potential has a finite negative minimum away from zero.

For `H_N=N^-1 sum_(i<j) g_ij`, differentiation in both particle coordinates gives `B=-grad H_N` and `Delta_(4N) H_N=c(N-1)`. The factor two in the Laplacian is essential. The shifted energy `H_N-(N-1)g_*/2` is a sum of nonnegative terms, diverges at every partial collision and has compact sublevels in the punctured configuration space.

Smooth local cutoffs therefore patch by pathwise uniqueness. Up to an energy-height exit the Itô coefficients are bounded, the stopped stochastic integral is square integrable, and expectation of the shifted energy plus total-force action is bounded by its initial mean plus `nu*c*(N-1)*T`. For a random initial point with energy above the cutoff, the stop is zero and the integrand vanishes; there is no unbounded-initial-state martingale assumption. Exit probabilities tend to zero. The resulting global continuous singular path has positive minimum pair separation on every fixed finite horizon, almost surely, with no deterministic separation uniform over starting points or N.

Fatou first supplies finite expected total-force action. Consequently the energy stochastic integral has an integrable bracket and is a true L2 martingale. Isometry on the omitted stopped interval gives L2 convergence of the stopped martingales; pathwise localization then yields the exact identity

\[
H_N(X_t)+A_t=H_N(X_0)+\nu c(N-1)t+E_t,
\qquad \langle E\rangle_t=2\nu A_t.
\]

All terms are integrable. No second moment of the initial energy is needed. The full drift square retains its triple cross terms; an individual force-square bound is neither inferred nor required.

The independently checked floor below is `H_N>=-C*sqrt(N)`. Since initial iid Haar gives `E H_N(X_0)=0`, taking expectation gives

\[
\mathbb E A_T\le C\sqrt N+\nu c(N-1)T,
\qquad
\mathbb E\sup_{t\le T}|E_t|^2\le8\nu\mathbb E A_T.
\]

On a critical window, `nu*sqrt(N)<=lambda_lower^-1` and `nu^2*N<=lambda_lower^-2`, giving a bound uniform in N and in every beta in that window. This step does not use the stronger expected-energy sign from smooth free energy.

For the auxiliary fixed-N density passage, the heat drift has divergence at least `-c(N-1)`. For each continuous Brownian signal its smooth flow is a diffeomorphism, with Jacobian at least `exp(-c(N-1)t)`, because the additive noise disappears from its initial-point variational equation. Change of variables against initial Haar and Brownian averaging gives the claimed finite-N domination. The singular path's positive minimum separation and local C1 convergence of the heat forces give same-noise uniform path convergence by Gronwall, at fixed N,nu,T. Passing the measure inequality first on continuous tests, then on open sets and Borel sets, is legitimate. This proves actual L1 integrability of a Haar-integrable configuration source; its exponential N-dependence is never used as a uniform estimate.

Common translation and particle-permutation equivariance, together with uniqueness and the exact initial law, give Haar one-body marginals and exchangeability. They give no positive-time independence or two-time density assertion.

## 4. Pointwise source majorant and sharp floor

For `w_r=(1-exp(-u/r))^6` and `psi_r=1-w_r`, direct differentiation yields `0<=u*w_r'<=6*w_r`. The retained Fourier coefficient is positive and decays at fixed r like `|k|^-14`; hence the required retained kernel is C2 and the Fourier sums used below are absolutely convergent. The discarded heat integral is nonnegative with mass `c_r=C_*r`, and `0<=g_r(0)<=C/r` for `0<r<=2`. The decomposition is pointwise off zero and in Haar L1. It assigns no singular diagonal.

Exact ordered/deleted summation gives

\[
\frac{H_N}{N}=F_r+V_r-\frac{g_r(0)}{2N}
                         -\frac{N-1}{2N}c_r.
\]

The two quantities `F_r,V_r` are nonnegative for distinct reasons: positive retained Fourier coefficients and pointwise positivity of the discarded heat integral. Choosing `r=N^-1/2` proves the sharp-order floor. This argument is independent of the particle law and does not assert positivity of an arbitrarily weighted Coulomb kernel.

The logarithmic derivative calculation is valid with no boundary term: differentiating `u*w_r(u)*exp(-b*u)` and integrating gives

\[
b\int u w_r e^{-bu}\,du
=\int w_r e^{-bu}\,du+\int u w_r'e^{-bu}\,du
\le7\int w_r e^{-bu}\,du.
\]

With `b=c*xi^2`, this proves the slope bound 14. For nonzero lattice vectors, let `u=min(|k|,|l|)>=1`, `R=max(|k|,|l|)` and `q=k-l`. Then `a(u)/a(R)<=(R/u)^14`, `a(u)-a(R)<=14*a(u)*(R-u)/u`, `R-u<=|q|`, and `R/u<=1+|q|`. Bounding the vector numerator by `|q|a(u)+R(a(u)-a(R))` gives precisely a permissible bound `15|q|(1+|q|)^8`. There is no comparison of exponentially small neighboring heat-cutoff coefficients.

For the retained smooth source the diagonal is zero. The centered full product therefore equals the literal deleted statistic with both backgrounds. Its Fourier expansion and symmetrization give candidate (3.3), including its sign and half factor. Absolute summability follows already from `sum |k| a_r(k)<infinity`, bounded Fourier coefficients of the finite signed measure, and summable coefficients of the smooth vector field. For each shift q, Cauchy--Schwarz on `sqrt(a_r(k))*|rho_hat(k)|` gives the stated bound by `F_r`. With `v=grad f_t`, the constant is bounded by a fixed multiple of `sum (1+|k|)^10 |h_hat(k)|`, because the exact backward multipliers have modulus at most one.

For the discarded source, each periodized Gaussian summand obeys

\[
\operatorname{dist}(z,0)|\nabla p_u(z)|\le C p_{2u}(z).
\]

This follows from `dist(z,0)<=|z+n|` and a bounded scalar Gaussian ratio. The torus mean-value estimate for the vector field then gives `|J_(g-g_r)|<=C||Dv||_infinity Q_(2r)`. Substitution of `a=2u` produces the harmless factor one half in dimension four, absorbed in C; the scale is exactly `2r`. The three absolute statistic contributions are bounded by `V_(2r),c_(2r),c_(2r)/2`, respectively. The double background in fact vanishes, but is not dropped from the bound before its computation.

Applying the positive decomposition at both r and 2r bounds `F_r+V_(2r)` by `2H_N/N` plus the explicit errors. An overall constant absorbs the two without replacing a negative energy by its absolute value; enlarging a fixed G makes the final brace nonnegative. Since `r=N^-1/2` and `2r<=sqrt(2)<2` for every N>=2, the required scale range covers all these choices. The result is the genuine configurationwise bound

\[
\sqrt N\,|P_N[J_t](x)|\le C_h\bigl(G+H_N(x)/\sqrt N\bigr).
\]

Combining this with the exact energy identity and nonnegative action, and integrating in time, proves candidate (3.7). In particular one can take `a=C_h*T` and `A=C_h*T*(G+c*T/lambda_lower)` on the window. Neither depends on N or the convergence rate of a critical sequence. The nonzero A is not a source-decay estimate.

## 5. Initial clipping, bad-event cost and the endpoint identity

The local coefficient-one singularity and bounded negative part give the three bounds in candidate (4.1): high-level probability `O(L^-2)`, truncated second moment `O(1+log L)`, and clipping mean in `[-C/L,0]`. The logarithm is necessary at this threshold. On an open set where all other pairs remain separated, the actual unclipped energy square contains a positive multiple of `r^-4` and integrates against `r^3 dr`; its second moment is infinite at each finite N>=2. This is a valid attack on a direct squaring of (3.7), but is not used in that way by the submitted proof.

For `g^[L]=min(g,L)-m_L`, every Haar row is exactly zero. Under iid initial Haar, two distinct unordered pairs with no common label have zero covariance by independence. If they share one label, conditioning on that coordinate gives the product of two zero row integrals; this also vanishes. The identical-pair term is the sole survivor. Thus

\[
\mathbb E(H_N^{[L]})^2=\frac{\binom N2}{N^2}\|g^{[L]}\|_2^2
=\frac{N-1}{2N}\|g^{[L]}\|_2^2.
\]

There is no assertion that overlapping pairs are independent. The fresh diagnostic produces a nonzero overlap covariance if the clipping is not recentered.

At `L_N=L_0*N^2`, the union bound has exactly `N(N-1)/2` pair events, so the bad-event probability is `O(N^-2)`. On its complement,

\[
H_N(X_0)=H_N^{[L_N]}+\frac{N-1}{2}m_{L_N}\le H_N^{[L_N]}.
\]

The sign of the mean and its `(N-1)/2` coefficient are correct. Only an auxiliary initial random variable is clipped; the actual dynamics and source are unchanged.

The force row has the sign

\[
\int J^f(x,y)\,dy=-c(f(x)-\bar f),\qquad\iint J^f=0.
\]

Indeed the force has zero Haar mean, leaving `-K*grad f`; the distributional divergence, including its atom and compensation, identifies this convolution. Empirical Itô with the two orientations of each pair gives the half factor. The full backward equation cancels the complete linear response, yielding `S_N=D_N-L_N` in the notation of the frozen THM048 card. The candidate denotes this random endpoint defect by `Delta_N` and subsequently reuses `D_N(h,T)` for its second moment; this local notation is unambiguous in the candidate and does not change the frozen random variable.

The drift passage is legitimate either by the fixed-N density bound and Haar L1 source, or by the pointwise energy bound and integrability just proved. The bounded one-body martingale integrands pass in L2. Thus this is a genuine identity of the time-integrated singular source. At fixed N, the endpoint is bounded by `2||h-bar h||_infinity*sqrt(N)` and the one-body martingale has finite variance; consequently both source and endpoint are actual L2 random variables even at the indices with beta<1.

The exact one-body bracket and variance are candidate (5.3). Pathwise gradient contraction follows from `Q_a h=bar h+exp(-ca) heat_(nu a)(h-bar h)`, and the expected bracket uses only the actual one-body Haar law. On the critical tail, `q_N=O(nu_N)=O(N^-1/2)`. The energy/one-body cross bracket is exactly `-(2nu/sqrt(N))*sum B_i.grad f(X_i) dt`; total-force action and bounded gradients make it integrable. No endpoint/noise independence or zero cross term is used.

The bad event is measurable at time zero. Multiplying the stochastic integrand by its indicator and applying ordinary isometry proves candidate (5.6), without independence of the future process from the event. The endpoint square costs only `O(N)` on this event. Multiplying by its `O(N^-2)` probability gives the decisive `O(N^-1)` bad-event contribution. The fresh exact martingale tree shows a nonzero failure when the event is instead terminal, and a nonzero endpoint/noise cross term.

## 6. Whole THM048, including every equivalence and finite prefix

On the good initial event the squared overshoot is bounded by `2a^2/N` times the sum of the clipped-energy square and energy-martingale maximum square. Their expectations are `O(1+log N)` and `O(1)`, respectively. On the bad event the endpoint/martingale bound and time-zero isometry give `O(N^-1)`. These prove the stated squared-overshoot estimate with exactly its allowed dependence. The argument also proves the constructor's stronger finite-window form: all inequalities hold for every N>=2 in the fixed window; the stated extra lower threshold ensures beta>=1 when identifying the original sigma with sqrt(N).

For `x>=0`, the scalar inequality `x^2<=2Ax+2(x-A)_+^2` gives candidate (6.4), while Cauchy--Schwarz gives the reverse vanishing implication. This includes A=0. For any R>2A, `x^2 1_(x>R)<=4(x-A)_+^2`. Given an error tolerance, first choose a tail index so the overshoot expectation is uniformly small on that tail; then let R grow to control each of the finitely many preceding L2 variables. This proves squared uniform integrability along every admitted sequence, with no universal waiting index or convergence-rate assumption.

For completeness, the endpoint clauses added explicitly in the root-frozen card follow without a new assumption. Since `E|L_N|^2=q_N->0`, the family `|L_N|^2` is uniformly integrable by the same tail-index/finite-prefix argument. Squared UI is preserved on addition: for nonnegative u,v,

\[
(u+v)^2\mathbf1_{u+v>R}
\le4u^2\mathbf1_{u>R/2}+4v^2\mathbf1_{v>R/2}.
\]

Applying this to `D_N=S_N+L_N` proves squared UI of the endpoint family. Also

\[
|\mathbb E|D_N|-\mathbb E|S_N||\le\sqrt{q_N},
\qquad
\big|\|D_N\|_2-\|S_N\|_2\big|\le\sqrt{q_N}.
\]

Therefore the four frozen L1/L2 limits are equivalent. This is a direct consequence of the exact identity; no endpoint/noise cross term is discarded. The finite initial segment has positive finite diffusivity, so its L2 integrability already established is sufficient for both UI assertions. Its possibly different `min(beta_N,1)` does not change any limit or positive-limsup criterion, because beta_N tends to infinity along every admitted critical sequence.

All the relevant nonnegative quantities are uniformly bounded on the tail. Failure to converge to zero is exactly positive limsup. The inequalities above and (6.4) transfer that failure in both directions, even for oscillating sequences. When T=0 or h is constant, source, endpoint and martingale vanish identically, without division by A or T. N=2 remains within every finite-N construction and clipping calculation.

The stronger ancillary statement that any weak subsequential source limit is supported in `[-A,A]` also follows: `P(|S_N|>A+delta)<=epsilon_N/delta^2`, and the tail bound gives tightness. It supplies no reason for that support to collapse to zero.

## 7. Whole THM049: exact actual correlation identity

The corrected modal kernel `j_k=J^(e_k)+c(e_k(x)+e_k(y))` is symmetric and has zero Haar rows. Counting N-1 partners for each coordinate gives

\[
P_N[J^{e_k}]=U_N^k+\frac cN Z_k,
\qquad
dZ_k=-\widetilde a_k Z_k\,dt+U_N^k\,dt+dM_k.
\]

The finite-N correction has positive sign in the source and reduces the damping by c/N. It is not an initial centering change. The direct complex Itô bracket is

\[
d\langle M_k,\overline M_l\rangle
=\frac{2\nu c}N(k\cdot l)Z_{k-l}\,dt,
\qquad
d\langle M_k,\overline M_k\rangle=\frac{2\nu\ell_k}N\,dt.
\]

The unequal-mode bracket can be nonzero pathwise, as the fresh diagnostic confirms. For each fixed N and horizon, `j_k` is Haar L1 in its current pair coordinates and has a fixed-N density majorant. Multiplication by a current or initial character has modulus one. Thus every A2,A3,B2,B3 is finite, its time integral is absolutely integrable, and Fubini is licensed without any two-time density or product-law premise. At N=2 no A3 or B3 is defined or used.

In `N E(U_N^k conjugate(Z))`, the coefficient before the raw triple sum is `1/(2N^2)`. There are `2N(N-1)` overlapping triples and `N(N-1)(N-2)` distinct triples. Symmetry identifies the two overlapping expectations, including when the third slot is at time zero. Consequently the exact weights are `(N-1)/N` and `(N-1)(N-2)/(2N)`, for both F and G, precisely as frozen. They do not rely on joint independence.

The product rule has two evolving response slots in the current/current variance and only one in the current/initial covariance. The bounded current mode makes its martingale product integrable; the initial mode is bounded and time-zero measurable. It follows in the absolutely-continuous-in-time sense that

\[
V_N'=-2\widetilde a_k V_N+2\nu\ell_k+2\Re F_N,
\qquad
R_N'=-\widetilde a_k R_N+G_N,
\qquad V_N(0)=R_N(0)=1.
\]

The initial values use iid Haar and k nonzero. Solving these equations and substituting in `V_N(T)+A_k^2-2A_k Re R_N(T)` gives exactly the frozen identity, including the unequal exponential weights on F and G. All signs, coefficients and conjugations check. In particular the first explicit term is the square of the difference of two real exponentials.

For N>=2, `a_tilde>=c/2`. More explicitly,

\[
0\le e^{-\widetilde a_kT}-e^{-a_kT}
=e^{-\widetilde a_kT}(1-e^{-cT/N})\le cT/N,
\]

and the noise term is between zero and `2nu_N|k|^2`. This proves `O(N^-2)` and `O_k(N^-1/2)` with the declared critical-tail constants. At T=0 both terms vanish. Nonnegativity of the actual endpoint variance gives `(-Re Rcal_N)_+` at most half the sum of these explicit terms, hence o(1).

The ancillary formula (8.8) also checks. The current same-label contribution to `N E|Z_T|^2` is one; its distinct-label contribution is `(N-1)C_N`, which is real by exchangeability of labels 1 and 2. The two-time covariance has one same-label term and N-1 distinct-label terms. The initial distinct-label expectation vanishes exactly by iid Haar. No missing real part or hidden initial correlation remains.

## 8. Every fixed smooth real test and exact negation

For a fixed k, the complex endpoint defect splits into the real cosine and sine defects, and its squared modulus is exactly their two squared defects added together. Thus the complete THM048 reconstruction makes vanishing of the original L1 sources for these two fixed real tests equivalent to vanishing of the modal endpoint variance. By the exact identity and the two explicit o(1) terms, the latter is equivalent to `Re Rcal_N(k,T)->0`. This proves necessity for every mode and sufficiency for every finite Fourier polynomial.

The passage to an arbitrary fixed smooth real test does not assume uniform L2 summability. The pointwise majorant is linear in a fixed order-ten Fourier seminorm of h. Its expectation can be bounded using `E H_N(X_t)<=nu*c*(N-1)*t`, which follows directly from the true energy identity and nonnegative action. Consequently on the critical tail,

\[
\mathbb E|S_N(e_k,T)|\le C_T(1+|k|)^{10}
\]

with the allowed window dependence. This estimate is for the actual original source; eventually sigma_N equals sqrt(N). At each of the finitely many remaining indices, the same argument has a finite constant depending on that index's finite diffusivity. Taking their maximum with the tail constant supplies a polynomial L1 majorant for the entire fixed sequence if desired; it introduces no universal waiting index. Alternatively, the asymptotic dominated-summation argument needs only the critical tail.

For the Fourier series of h, `sum (1+|k|)^10 |h_hat(k)|` is finite. The same seminorm applied to Fourier tails tends to zero. Thus finite partial sums converge to h in the original source's L1 norm uniformly on the critical tail, and mode convergence passes to that same smooth h. This explicitly supplies the interchange, rather than inferring it from fixed-mode convergence. Constants contribute zero.

For the full-target negation, if every fixed modal correlation tends to zero, the preceding argument forces every fixed smooth real source to tend to zero. Therefore an admitted full-target failure forces a failure at a fixed nonzero mode for the same horizon and sequence. Since the negative part of its real correlation is o(1), that failure is positive limsup, not persistent negative oscillation. Conversely positive modal correlation limsup gives positive modal endpoint-variance limsup. The two real component variances are nonnegative, so at least one fixed cosine or sine has positive limsup (taking a subsequence if needed). THM048 transfers this to positive limsup of that test's original expected absolute integrated source. Neither the test nor the horizon depends on N. No witness is asserted to exist.

These arguments establish every clause of THM049 and its exact quantifiers. A rate of convergence of lambda_N, a static-law replacement, or a separate bound on any individual summand of F or G has not entered.

## 9. Ancillary and adversarial dispositions

| Challenge or additional claim | Result and limit |
|---|---|
| Square the initial energy or instantaneous source | The close-pair radial divergence defeats this route. The submitted good/bad-event proof avoids it. Infinite instantaneous variance is not confused with infinite variance of the time integral. |
| Omit recentering, the overlap count or the clipping mean | Each changes an exact coefficient; fresh finite-group witnesses are nonzero. The submitted formulas retain all of them. |
| Apply conditional isometry to a terminal event | False in a fresh exact martingale tree. The submitted event is time-zero measurable. |
| Delete the energy martingale or endpoint/noise cross term | Invalid. The report retains the actual bracket and uses inequalities valid with all cross terms. |
| Lose uniformity at critical coupling or N=2 | The scale range, window constants and finite-label formulas all cover N=2. Three-label terms are absent there. |
| Obtain a false full-test conclusion by pushing mass to higher modes | The order-ten L1 Fourier majorant rules out that escape for each fixed smooth h. No L2 Fourier-summability assumption is made. |
| Infer source decay from squared UI | Invalid; a bounded nonzero scalar sequence satisfies a zero-overshoot estimate. Candidate (8.7) remains the missing dynamic cancellation. |
| Constructor's radial formulas (7.1) | PASS. For Y=U^-1/2, density `2Y^-3` on Y>=1 gives the stated tail, mean, clipped variance and capped square exactly. The fresh diagnostic checks formal logarithmic coefficients and rational parts, with no floating tolerance. |
| Constructor's stronger finite-window CLAIM_CARD | PASS as a separately identified ancillary statement. It is not substituted for the two frozen cards. |
| Earlier reports, audits and constructor diagnostic aggregate counts | No status promotion or execution certification. Their necessary elementary inputs were reconstructed; their other claims and withheld artifacts remain outside scope. |

No source or proof repair was made. The first remaining unproved target line is candidate **597–599, equation (8.7)**, the actual signed correlation cancellation. Its openness is compatible with the two PASS verdicts because both theorems are integrability/equivalence statements.

## 10. Fresh computation, evidence and issuance

`hostile_exact_diagnostic.py` was written in this context without opening any prior checker or constructor result. It uses Python 3.9.6 standard library only, exact rational and Gaussian-rational arithmetic, and formal logarithmic coefficients for the radial integrals. There is no random sampling, numerical SDE approximation, floating tolerance or installed dependency.

The first execution passed **1,906 assertions in 33 categories**, with **20 distinct nonzero mutation witnesses**. These include the retained Fourier commutator, positive heat moments and logarithmic slope, energy self/background coefficients, canonical clipping and overlapping pairs, bad-event count, conditional isometry, shared endpoint noise, radial integrals, current/current and current/initial overlap counts, deleted modal correction, nonzero unequal-mode bracket, response slots, correlation-solution algebra and scalar tail implications. The full JSON records all category counts and witnesses. The diagnostic's finite groups and abstract covariance algebra are expressly coefficient tests; their configurations and arbitrary algebraic values are not claimed to be admitted singular particle laws or covariance matrices. The analytic singular passages above are not replaced by computation.

The companion directory is `AUDITS/HOSTILE/ROUND_025_SOURCE_UI_HOSTILE_ARTIFACTS_20260918_085716_UTC`. It contains this report, all 15 exact input copies, original input manifest, input-verification record, complete source/read/exposure record, fresh diagnostic and full result, run history, README, exact member/digest inventories and a portable read-only verifier. The named sibling archive and seals authenticate the issued bytes; archive members are unique safe relative regular files with read-only permissions. Corrections must be distinct superseding artifacts.

**AUD070-ADM01:** TASK109 says 663 candidate lines; the matching supplied candidate has 687. Severity: administrative only. Confidence: exact byte/line count. Disposition: the entire 687-line candidate was reviewed, and the discrepancy was reported to root; no frozen input changed. It has no mathematical downstream consequence.

No other source, constructor code/result, current blind report, current audit narrative, canonical STATE, Git history, memory file, external source, other worktree or nonallowlisted linked record was opened. Ambient memory summaries and historical statuses inside allowed exposure prose were not mathematical evidence. No children, installation, commit, push, remote change or canonical edit occurred. Only the assigned report and named artifact/seal paths were written. No TeX source was changed or compilation claimed; the formula review was of the complete Markdown source. The final conversational handoff contains no mathematical LaTeX.

Root's next action is to compare both whole verdicts and every reconstructed step against the withheld fresh reconstruction, match exact accepted source gates and then decide promotion. This report does not close THM046 or authorize any broader theorem, publication or remote action.
