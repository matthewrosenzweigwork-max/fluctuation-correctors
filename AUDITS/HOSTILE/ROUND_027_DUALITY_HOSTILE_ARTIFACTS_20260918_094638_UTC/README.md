# AUD076 evidence packet

This packet supports the independent hostile review of all of frozen THM052(A)–(C) and every mathematical ancillary assertion in the complete 99-line root proof. It is the exposed hostile axis, not the separately withheld blind axis. The issued verdict is PASS for A, B, C and their whole mathematical conjunction; no first mathematical failure was found. Root comparison and promotion remain separate.

Read `REVIEW.md` first. `INPUTS/` contains all eleven exact allowed inputs, and `INPUT_SHA256SUMS.txt` is the exact supplied input manifest. `INPUT_VERIFICATION.json` records the successful pre-review and copy checks. `READ_EXPOSURE_HISTORY.md` records the full boundary, actual reads, truncated-output recovery and tool/execution history. No absent earlier gate or constructor diagnostic was used as a premise.

`DIAGNOSTICS/fresh_exact_diagnostic.py` is fresh standard-library code using exact rational arithmetic. Its first execution passed 1,239 checks and recorded 14 nonzero mutation discrepancies. Its complete stdout, empty stderr and return code are retained. This supports coefficient, event and orientation checks; it does not numerically certify the singular diffusion, continuum path law, or unresolved modal decay.

The portable read-only verification command, from any working directory, is:

    python3 -B /absolute/path/to/ROUND_027_DUALITY_HOSTILE_ARTIFACTS_20260918_094638_UTC/verify_readonly.py

Use Python 3.8 or later with its standard library. The command reads this directory and its named sibling report/archive/seals, validates all inventories and hashes, rejects links/nonregular members and unsafe archive names, compares archive bytes without extraction, checks every frozen input copy, verifies the issued report identity, and reruns the diagnostic in a no-bytecode subprocess with a timeout. It writes no files. The sibling files are:

- `ROUND_027_DUALITY_REVIEW.md` — assigned issued report, byte-identical to `REVIEW.md`.
- `ROUND_027_DUALITY_HOSTILE_ARTIFACTS_20260918_094638_UTC.tar.gz` — complete regular-file archive.
- `ROUND_027_DUALITY_HOSTILE_ARTIFACTS_20260918_094638_UTC.SEAL.json` — full packet and archive regular-member inventory, digests and report identity.
- `ROUND_027_DUALITY_HOSTILE_ARTIFACTS_20260918_094638_UTC.VERIFY.json` — retained pre-issuance verification execution and its complete result.
- `ROUND_027_DUALITY_HOSTILE_ARTIFACTS_20260918_094638_UTC.SEAL.sha256` — detached digests for the archive, seal, report and verification receipt.

`INVENTORY.json` inventories every regular packet file other than itself. Its own digest and the complete archive inventory are in the sibling seal; thus this explicit self-exclusion leaves no unsealed packet file. The archive contains files only, each under the one packet-directory prefix, with no directory entries, symlinks, hardlinks, devices, duplicates, absolute paths or parent traversal. The detached digest file does not hash itself; it is an integrity anchor, not a cryptographic signature or proof of an immutable filesystem. Issued files are mode 0444 and packet directories mode 0555. Corrections require a separately named superseding packet.

`assemble_and_seal.py` records the exact issuance procedure. Its single intended execution takes the already written report and packet, copies the report, inventories the complete packet, creates the safe archive and seal, freezes permissions, executes the read-only verifier once with `--pre-receipt`, writes that result as the sibling verification receipt and writes the detached final digests. The retained receipt documents that pre-issuance execution; normal verification additionally validates the receipt and detached digests. No self-referential hash is claimed.

No TeX source was changed. No source, canonical ledger, dependency, Git commit, branch, remote or other worktree was modified. The first unresolved scientific line remains the modal conditional-defect decay expressly excluded from THM052. The root must check its named earlier-source/gate version matching and compare both complete independent axes before deciding promotion.
