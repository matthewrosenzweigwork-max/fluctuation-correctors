# Whole new bounded claim for independent review

TASK111; SELF-CHECKED only. Root owns new campaign identifiers and promotion.
All notation, finite-N domains, source reconstruction and proof are in
REPORT.md. No current R26 constructor or audit output was available.

Use precisely the actual iid-Haar d4,s2 coefficient-one Coulomb gradient
particles, positive finite diffusivity, initial independence from the
Brownian drivers, and literal mean-field-centered source in THM046/THM049.
For each fixed nonzero lattice mode k, finite T, and fixed positive finite
critical window lambda_lower <= beta/sqrt(N) <= lambda_upper, there is a
finite C depending only on these data and the fixed kernel such that for
every N >= 2 and all 0 <= t <= T:

1. abs(A2(t)) <= C N^(-1/2).
2. abs(B2(t)) <= C N^(-1/4) sqrt(1+log N).
3. The exact two-label contribution to Rcal_N has absolute value bounded
   by C N^(-1/4) sqrt(1+log N). Its three-label complement keeps precisely
   (N-1)(N-2)/(2N) times the original weighted integral of A3 and B3, and is
   defined as zero when N=2.
4. The frozen signed-correlation target and its positive-limsup negation
   are respectively equivalent to vanishing and positive limsup of this
   three-distinct-label signed complement along every admitted sequence.

The exact negation is an admitted fixed datum/window for which no such C
works for all the specified N,beta,t, or failure of the exact decomposition
or the asymptotic equivalence. Failure of a proposed proof is not a
negation. The original full target and an admitted witness remain open.

Load-bearing new lemmas: current-pair small-ball probability at radius R
is at most C(R^4+1/N); the second moment of the heat-truncated source at
heat scale r is at most C(1+log(1/r)+1/(N r^2)); the exact current-overlap
Fourier identity has self subtraction -2 d_(k,r)/(N-1), with heat response
d_(k,r)=4 pi^2 exp(-4 pi^2 r |k|^2). The cutoff is a fixed small constant
times N^(-1/2), applied only to the observable. Actual dynamics are never
cut off in the final estimates.

The claim does not assert triple-correlation decay, a new L1/L2 theorem,
instantaneous singular source variance, product law, uniform N-body
density, finite hierarchy closure, Gaussianity, or a new law class.
THM048 and the L1-to-L2 equivalence in THM049 are not premises; the needed
exact modal identity is reconstructed explicitly.
