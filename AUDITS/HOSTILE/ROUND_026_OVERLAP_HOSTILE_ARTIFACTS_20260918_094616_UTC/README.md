# AUD074 whole THM051 hostile packet

Whole mathematical verdict: PASS for all frozen THM051(A)–(D) and all
mathematical assertions in the complete TASK111 report. No repair or failed
mathematical line was found. This is the hostile-review axis only. Root
compares the separate entire reconstruction and source gates before promotion.
The original source mission and weighted three-label cancellation remain open.

Start with REPORT.md. The authoritative issued report is the identically
hashed sibling ROUND_026_OVERLAP_REVIEW.md.

The INPUTS directory contains all 15 exact allowed source copies.
INPUTS_ORIGINAL_SHA256SUMS.txt is the required manifest. The two verification
JSON files record initial/copy and final original/copy checks.
EXPOSURE_AND_SOURCES.md and RUN_HISTORY.md disclose the complete source and
execution boundary, including output truncations and a rejected file-creation
request.

diagnostic.py is fresh standard-library supporting code. RUN_001_STDOUT.json,
RUN_001_STDERR.txt and RUN_001_PROCESS.json preserve the entire first run:
187,032 assertions, 25 categories, 20 nonzero mutation witnesses, exit zero.
Its finite Fourier/radial computations are exact; its declared heat sweep is
double precision. None substitutes for the analytic or actual-law proof.

From any directory with Python 3, run the following, replacing PACKET with
this extracted directory path:

    python3 -B PACKET/verify.py
    python3 -B PACKET/verify.py --rerun-diagnostic

The verifier only reads. It validates the complete directory and archive
member sets, regular types, safe paths, read-only modes, sizes, SHA-256
digests, source manifest, report identity and sibling checksums. It never
extracts the archive. The optional rerun executes the diagnostic with
captured memory output and compares all mathematical results with the saved
run, permitting only insignificant floating-point sweep variation.

Keep the packet, its identically named .tar.gz, _INVENTORY.json,
_SHA256SUMS.txt and the issued report together in one parent directory.
The final _VERIFICATION.json and _VERIFICATION_SHA256SUMS.txt are sibling
read-only attestation files from the issuing verifier execution. The main
checksum seal covers the report, archive and complete inventory. The extra
verification seal covers that verifier result and the main checksum seal.
Neither unkeyed checksum file is a cryptographic author signature.

No canonical/input edits, dependency installations, commits, pushes or remote
changes were made. All issued files are immutable/read-only; any correction
must have a separately named report and packet.
