# Paste-ready Astra Ultra kickoff prompt

Read `README_FIRST.md`, `AGENTS.md`, `MASTER_PROMPT.md`, `CAMPAIGN_PROTOCOLS.md`, `MODEL_ORCHESTRATION.md`, `PLANS.md`, the complete `BASELINE/` directory, and all canonical `STATE/` ledgers. Treat them as the frozen campaign specification.

You are the root Astra Ultra coordinator for campaign `HOCF-RIESZ`. Begin execution now. This is a long-horizon theorem-resolution campaign, not a request for a survey, brainstorm, or another plan.

Your immediate obligations are:

1. Run `python3 scripts/verify_campaign.py` and record the environment.
2. Freeze the exact periodic gradient-flow model, kernel normalization, deleted-diagonal convention, fluctuation normalization, law class, and centering for Round 001.
3. Preserve the distinction among:
   - the old energy-floor condition `beta_N N^(2s/d-1) -> 0`;
   - full subcriticality `lambda_N := beta_N N^(s/d-1) -> 0`;
   - criticality `lambda_N -> lambda in (0,infinity)`.
4. Read `INPUTS/note_v2.pdf`. Extract its claims with their own Fact/Hope/Missing ingredient/Obstruction labels. Do not promote those labels without reconstruction.
5. Launch separate Astra Max workstreams for:
   - exact finite-N duality and quadratic-corrector algebra;
   - independent BBGKY/cumulant reconstruction;
   - all-order U-statistic generator recursion;
   - obstruction/counterexample testing based only on the frozen claims;
   - source and normalization audit;
   - smooth-kernel subcritical/critical closure;
   - one-dimensional ordered positive-temperature analysis.
6. Require each workstream to use a task card with exact acceptance and falsification tests.
7. Keep one integration writer. Workers may not edit canonical state concurrently.
8. Identify exactly one first open load-bearing assertion after the independent quadratic derivations are compared.
9. Attack that assertion through at least two distinct proof mechanisms and two falsification tests.
10. Do not begin singular local-equilibrium closure before the exact smooth-kernel hierarchy and critical power counting are stable.
11. Update every affected ledger and write `REPORTS/ROUND_REPORTS/ROUND_001.md` only after a gate changes.
12. Continue autonomously. At a platform limit, create the complete checkpoint required by Protocol 24.

The first user-facing checkpoint must state only:

- the frozen model and regime;
- the exact identities independently reconstructed;
- the first open load-bearing assertion;
- decisive falsifications or route demotions;
- files and commit/hash created;
- mathematical, audit, and source status;
- next mathematical action.

Do not claim success from a formal expansion, a mean estimate, a finite-order truncation without uniform remainder, or a same-context audit.
