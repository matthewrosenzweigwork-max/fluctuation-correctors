# Certificates

Store exact, interval, symbolic, or finite combinatorial certificates that are shorter to verify than to discover. Link each certificate to a computation card and independent verifier.
