# R21 verification certificate

Root read the complete root construction and all527/249 lines of the fresh reconstruction/hostile reports, full source/exposure/preflight/README/verification records and portable verifiers. Exact whole comparison and final twelve-item hostile integration are AUDITS/ROUND_021_RECONSTRUCTION_COMPARISON.md and ROUND_021_GATE_INTEGRATION.md. Full prior source/gate bytes are matched in CERTIFICATES/OUTPUTS/round020_021_source_gate_match.json; preserved source qualifications are stated explicitly in the gate.

Executed read-only packet commands:

    python3 MEMORANDA/ROUND_021_CRITICAL_CUBIC_ARTIFACTS/verify_packet.py
    python3 AUDITS/BLIND_RECONSTRUCTION/ROUND_021_CRITICAL_CUBIC_BLIND_ARTIFACTS/verify_packet.py
    python3 AUDITS/HOSTILE/ROUND_021_CRITICAL_CUBIC_HOSTILE_ARTIFACTS/verify_packet.py --self-test

All pass with29/29/32 inputs and38/38/42 archive members. Complete input/output/outer manifests, safe member inventories, CRC and uncompressed bytes, original/copied bytes and issued read-only modes were checked. The hostile packet has44 files plus its report. No archive extraction was performed.

Fresh code-only temporary diagnostics reproduce the saved12531/117352/137147 exact-assertion results. The blind has72 whole smooth generator identities and18 detecting mutations. The hostile checks full generator/carré-du-champ/background identities, actual-generator terminal-zero time jets,12384 distinct admitted rational parameter pairs and10 coefficient/sign plus4 exponent mutations. All-real range proofs and singular analytic passages are in the written reports; finite tests do not certify them. Integrity controls reject9 blind and10 hostile adverse fixtures.

The first hostile stdout-byte comparison failed only because print(rendered) adds one newline to its already newline-terminated saved JSON. Root reran the documented --write inside a new temporary directory containing only the unchanged program; the resulting8045-byte RESULTS.json exactly matches the sealed result. Stdout is exactly that file plus one newline. This is an output-format distinction, with no failed mathematical assertion or evidence edit. Machine records round021_cubic_root_{rerun,verification}.json and round021_cubic_{blind,hostile}_root_{rerun,verification}.json preserve complete outcomes; the hostile record explicitly describes the initial failed comparison.

The final R20/R21 TeX/PDF is rebuilt and visually checked in the publication preflight. This certificate records analytic/evidence integration; staged-byte, protected-source and Git publication checks are separate.

Final accepted-status synthesis MEMORANDA/hocf_round020_021_20260918T070500Z.tex was built with latexmk -pdf -interaction=nonstopmode -halt-on-error -outdir=BUILD/round020-021. Complete final log read; all three final pages rendered and visually inspected. Final PDF:3 pages,194423 bytes. No undefined references/citations, overfull boxes, missing glyphs, clipping or defective page breaks. Canonical PDF and verbatim log saved; build success is not mathematical certification.
