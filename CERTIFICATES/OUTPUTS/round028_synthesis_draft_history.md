# R28 synthesis preparation

The first draft compiled as four pages but used primitive binomial notation (amsmath warning) and had one11.66335pt overfull inline definition. Replaced the primitive notation with standard binom and displayed that definition. The second build produced four pages183123bytes; the complete log and all four100dpi renders were inspected, with no remaining warning, reference/citation, missing-glyph or layout defect. This was explicitly a pending-gate draft. A later source-definition clarification precedes first use of J and P; final gate status, build/log/render review and canonical PDF copy remain required before publication. No issued constructor/audit evidence was altered.

A final source reread detected a malformed iid subscript in the editable synthesis (a newline followed by m iid). Corrected it to the standard upright iid subscript; no mathematical expression or sealed evidence changed. Rebuild succeeded, four pages183661bytes. This correction, source-definition insertion and final status require fresh final visual inspection before publication.

Final whole-gate status set after AUD080 verification, then fresh build and complete log inspection succeeded; all four final rendered pages inspected and canonical183721byte PDF plus verbatim log copied. No remaining layout or diagnostic warning.
