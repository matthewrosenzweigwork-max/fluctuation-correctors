# Root validation history — Round 028

All three constructor portable verifiers and independent fresh diagnostic reruns pass. The broader inventory helper initially mistook TASK119 OUTPUTS/PRESEAL_GIT_STATUS.txt for a checksum manifest because its filename contains SEAL. Eleven lines of literal Git status were therefore reported as invalid checksum syntax. Root read the entire snapshot and corrected only the temporary broad helper to exclude that exact non-manifest basename. No campaign verifier, source, issued evidence or digest was modified. The actual snapshot remains covered by the packet inventory and external archive seal. The corrected full validation is recorded separately.

Two discovery reads used guessed names TASK-118_ROUND027_KILLED_DUAL_HOSTILE.md and STATE/TASK_BOARD.md, which do not exist; exact names were resolved from the repository inventory. A guessed TASK121 exposure filename likewise did not exist; the actual EXPOSURE_READ_HISTORY.md is used. These were read-only path errors, with no missing source or altered evidence.

The first root reproduction wrapper displayed only the command output and omitted its returned running-session identifier. The process continued normally; root inspected its process identity and then all four completed verification/rerun JSON records. Both complete reproductions passed. No issuance script or evidence was rerun or changed.

Post-compaction root reading: two guessed AUD079 issuer/runner paths at packet root did not exist; file inventory resolved both under CODE and both full files were then read. The initial combined tool display truncated early AUD077 diagnostic lines; explicit 1–120 recovery completed the read. No evidence or code was changed. AUD079 archive has ten ordinary PAX long-path records; root supplements verification by allowing only exact safe path metadata and rejecting every other PAX override, not by falsely claiming no PAX headers. All 42 payload entries remain regular and no extraction occurs.

The broad root validator reported two missing-code paths in AUD077 ADMIN/EXECUTED_CODE_SHA256SUMS.txt because its generic bases omitted the documented packet-root base. Read the exact manifest/verifier semantics and added only that named manifest base to the temporary helper; immutable evidence was not changed. The dedicated full AUD077 verifier and independent root replay had already passed.

One root read attempted the not-yet-created ROUND_028_GATE_INTEGRATION.md and reported its absence; the complete gate was subsequently authored only after final AUD080 evidence passed. No prior proof was inferred from the absent file.

Final broad checker initially treated AUD080 SHA256SUMS.json as a text manifest. Its explicit AUD080 JSON schema now dispatches to size/digest checks; immutable evidence was never altered. The same scan detected one form-feed rendering defect in future R29 report equation7.2; a separate R29 erratum preserves the exact4/N coefficient and all sealed bytes. R29 artifacts remain outside the R28 publication scope.

Final staging initially named nonexistent STATE/BASELINE_SHA256SUMS.txt; git add exited128 without changing the index. The actual refreshed root SHA256SUMS.txt was identified from git diff and staged explicitly. No evidence bytes changed.
